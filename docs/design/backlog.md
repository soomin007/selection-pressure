# 백로그 (단기 후보)

> 앞으로 할 일만 적는다. 끝낸 항목은 지우고, 무엇을 했는지는 `session_logs/` 에 남긴다.
> 폰 검증 대기 항목은 여기 말고 최신 세션 로그의 "미해결/다음 할 일"에 있다.
> **형질/시스템 큰 로드맵(초음파·날개/지느러미/다리·독침/원거리 등)은 [`../ROADMAP.md`](../ROADMAP.md)가
> 단일 소스다. 여긴 그 로드맵에서 다음에 착수할 것 + 자잘한 튠만 끌어온다.**

## 다음 세션 착수
- [ ] **무리 방어 폰 체감 + 시각화(2026-07-14 구현 후 대기).** 뭉치면 포식자가 아예 안 노리는 규칙
  (`SIM.herdShield*`)이 들어갔다. 다산 초식 도달 2.0 → 4.0(10시드), 다른 프리셋은 소수점까지 불변.
  **화면에서 안 읽힌다** — "포식자가 안 오는 것"은 눈에 보이지 않는다. 무리가 방패를 받는 동안 표식
  (무리 둘레 옅은 링? 개체 발밑 연결선?)이나, 포식자가 무리 가장자리에서 **돌아서는** 연출이 필요.
  (`render/worldView.ts` = 레인 D 범위). 폰에서 "뭉치면 산다"가 체감되는지 확인 후 강도 튠.
- [ ] **다산 초식이 이제 굶는다(다음 병목).** 무리 방어로 잡아먹힘은 24 → 13 으로 잡혔는데 굶주림이
  4 → 28 로 올라왔다(개체가 살아남아 늘어난 만큼 먹이 경쟁이 커진다 — 건강한 압력이지만 승리는 1/10).
  다음 레버 후보: 순수 초식 채집 수입 · 먹이 재생 · 무리 전용 먹이 틈새. **단, 무리 채집(뭉치면 채집↑)은
  이미 시도해서 실패했다**(효과 0 — known_issues). 사인 집계를 먼저 보고 정할 것.
- [ ] **「느긋한 정찰자」가 이제 가장 약하다**(도달 3.7 · 잡아먹힘 37 — 여덟 중 최다). herding 24 라
  무리 방어를 못 받고, 시야만 높아 도망은 잘 가나 결국 잡힌다. 다산 초식 다음 순서.
- [ ] **육식 사냥꾼이 여전히 최강**(대륙 5.3). 구조적 원인: ① 사냥은 먹잇감이 야생 100마리라 사실상 무한한데
  채집은 유한한 먹이를 야생 초식 넷과 나눈다 ② **식성 특화 페널티가 30%뿐이라** 사냥꾼(식성 68)이 사냥
  효율 98% + 채집 효율 72% 로 **양쪽을 다 먹는다**. `dietSpecializationPenalty` 를 올리면 대멸종 필터가
  깨진다(프로브: 0.4 부터 경계) → predationEnergy 하향이나 채집 수입 상향 쪽을 프로브로 검토.
- [ ] **바다 야생종 확충.** 지금 바다에 사는 야생은 둘뿐(바다 풀뜯이·물고기 떼). 대양·군도 맵에서 육상
  야생 6종의 서식지가 줄어 생태가 한쪽으로 기운다. `WILD_ARCHETYPES` 직접 추가는 rng 스트림을 밀어
  밸런스가 통째로 이동하므로 **독립 rng 스폰**으로 넣어야 한다(known_issues).
- [ ] **대양에서 날개가 못 산다**(도달 1.5). 산이 1%라 고산 먹이가 없다. 나는 종이 얕은 바다 먹이를
  낚아채게 하면(물수리) 살아나지만 대륙에서도 날개가 강해지므로 프로브 필수.
- [ ] **물 → 뭍 진화 경로가 없다.** 수영을 내리는 카드가 없고, 물 전용 종(수영≥90)은 플레이어가 될 수
  없다(카드 상한 89). "바다에서 시작해 뭍으로 올라오는" 서사가 통째로 빠져 있다(2026-07-13 사용자 지적).
- [ ] **보스 층위 폰 체감 튠(2026-07-13 구현 후 대기).** 날개 빌드가 땅 보스를 통째로 회피하는 게 시원한지,
  대신 만나는 하늘 보스(큰수리·말벌 떼)가 그만큼 무서운지. 나는 종 아래 그림자가 "떠 있음"으로 읽히는지.
  큰수리 색(구릿빛 0xb5642a)이 하늘 개척자 내 종(황금 0xf0c840)과 충분히 구분되는지 — 안 되면 더 갈라야 함.
  상어는 수영 빌드로만 나오니 바다 프리셋으로 확인. 세부 후보: 말벌 속도 저항 0.9·큰수리 visionFlee 60.
- [ ] **하늘/물 보스 수가 얇다.** 나는 종이 만나는 보스는 큰수리·말벌 떼·독 안개 3종뿐(한 시대 관문 2개라
  당장은 충분하지만 시대를 거듭하면 반복된다). 수영 빌드도 상어 1종. 층위별 보스를 더 늘릴 여지.
- [ ] **상어가 작은 웅덩이에 갇힐 수 있다.** `mapHasSea` 는 nearestLargePassable 이 폴백으로 자투리 물을
  돌려주면 true 가 된다 → 바다가 없는 맵에서 상어가 웅덩이에 갇혀 무해해질 여지. 물 타일 수를 직접 세는
  판정으로 조여야 한다.
- [ ] **극단값(상한 0/100 도달) 설계.** 지금은 그냥 멈춤(clamp)+그 형질 카드 빠짐뿐이라 "최고조/바닥"에서
  무슨 일이 나는지 의도가 없다. 상한 100 에 닿으면 뭔가 의미 있는 상태(특화 완성 표식·부작용?)를, 하한
  0 도 마찬가지로 설계. 시뮬 공식엔 이미 바닥이 있으니(속도 0.4배 등) 그 위에 "읽히는 극단"을 얹을지 검토.
  (2026-07-12 사용자 지시 — 스탯 숫자+상한 100 개편 후속)
- [ ] **식성 효율 폰 체감 튠(2026-07-11 연동 후 대기).** 페널티 폭 `dietSpecializationPenalty`=0.3 은 대멸종
  필터가 안전한 상한이다(프로브: 0.4 부터 필터 경계). 폰에서 특화 유인이 약하면(잡식이 여전히 안전빵) 필터
  치사도(`globalColdLethality` 등)와 함께 올려야 한다(세션 7 방식). 「포식/초식 본능」 카드가 이제 사냥/채집
  효율 이득을 주는데, 드래프트·카드 도감에서 그 이득이 안 보인다(효율은 칩에 안 뜸) — UI 노출 검토.
- [ ] `stuckTicks` 재정의 — 지금 "이동량" 기준이라 물가 진동에서 안 걸려 **죽은 코드**다. "목표까지 거리가
  안 줄어듦"으로 바꾸면 살아난다(발동이 늘어 밸런스 이동 → 프로브 확인). 상세: known_issues.
- [ ] 물기 연출(`drawBite`)이 폰에서 읽히는지 + 추격 한 번에 2~7번 떠 시끄럽지 않은지 확인(세션 7).
- [ ] 「거인」·꾸밈·물기 개편 폰 체감 검증(세션 4·7).

## UI 폴리시 (진행 중)
- [ ] **개체 카드 이름 ↔ ★ 겹침(버그)** — 카드 헤더가 좁아(포트레이트 56 + 정보열 ~120px) 개체 이름과
  즐겨찾기 ★ 가 겹쳐 보인다. 이름 폭/줄바꿈 또는 ★ 위치 재배치로 해소. (`ui/creatureCard.ts`, 2026-07-08 사용자 지적,
  **2026-07-13 데스크톱에서도 재보고** — 즐겨찾기를 누르면 별표가 이름 글씨와 겹친다)
- [ ] **즐겨찾기 키 표식(B) 안 보임** — 데스크톱 키 칩(.ui-kbd)을 버튼마다 달았는데 개체 카드
  (`ui/creatureCard.ts`)의 ★·‹›·✕ 버튼에는 안 달았다. 키 자체(B 단골·←→ 개체 넘기기·Esc 닫기)는
  main.ts 관전 레이어로 이미 동작 — 표식만 없다. ★ 버튼에 B 칩(위 겹침 문제와 같은 헤더라 함께 해소),
  ‹›·✕에도 ←→·Esc 표식 또는 title 툴팁. (2026-07-13 사용자 지적)
- [ ] 신생아 amber "핑"이 초록 무리 속에서 충분히 읽히는지 폰 확인 — 약하면 대비/굵기 강화. (`render/worldView.ts`)
- [ ] 단골로 "바로 이동" 버튼(지금은 월드 금빛 별을 찾아 탭). 무리 형질 분포(평균·범위) 표시(§9)도 후보.

## 다음 세션 바로 후보 (2026-07-06 갱신 — 최근 대량 변경 후 상태)
- [ ] **런 보고서 폰 검증·확장** — 구현됨(결과 화면 "이 혈통의 기록": 개체 수 면적 그래프 + 형질 흐름
  꺾은선 + 연대기, `ui/runReportScreen.ts`). 폰에서 그래프 선 겹침·연대기 가독성 확인. 승리/정복(여러 시대
  누적) 런의 보고서도 점검. 여지: 형질별 개별 스케일 토글, 사망 원인 막대를 보고서에도, 챔피언 등재 표식.
- [ ] **보스 절차 아트 — 나중에 다시 논의(사용자 보류)**. 현재 상태: `drawBossShape`가 top-down 실루엣 8종
  (땅: chaser=질주 맹수·swarm=벌레·raider=뿔짐승·isolation=늑대·stalker=어둠 덩어리 / 하늘: raptor=큰 날개
  맹금·hornet=줄무늬 말벌 / 물: shark=유선형+초승달 꼬리), 눈썹 없음, 색 파생 굵은 윤곽선·큰 눈으로 생물과
  통일, 회전 안전. `npm run boss-preview`로 8종 스크린샷 확인. 남은 아쉬움(사용자 재논의 예정): 다리 달린
  4종이 다 벌레처럼 뭉뚱그려질 여지, 작은 크기 다리 굵기, stalker 별 모양, raider 뿔 각도, 상어 가슴지느러미
  가 별 모양으로 읽힐 여지.
- [ ] **최근 변경 폰 검증·튜닝** — 한 번에 많이 바꿔 체감 확인 필요: 형질 200 스케일 성장 속도
  (`cards.ts` CARD_GROWTH_SCALE 0.6), 카드 수치=실제값 동기화, 프리셋별 카드 풀(cardRedundant), 언락 레벨
  임계(`meta.ts` 3/5/7/9/12), 시대 난이도 곡선(`config.ts` eraDifficultyStep 0.22), 챔피언 등장, 스킵 보상
  (draftSkipBrood 2), 승리·정복·멸종 연출 강도(momentOverlay), 수동 카메라·줌·"한 마리 관찰". 약하면/과하면 상수 조정.
- [ ] **시대 보상 폰 튠** — 구현됨(시대 넘을 때 강화 카드 3장 중 1 선택, 정상 ×2 강도, `config.ts`
  eraRewardBoost 2). 폰에서 보상 도약이 난이도 상승(+22%/시대)을 기분 좋게 앞서는지 체감 후 강도 조정.
  약하면 boost↑, 너무 세 후반이 시시하면 boost↓ 또는 시대별로 감쇠.
- [ ] **메타 진행(플레이어 레벨) 폰 튠** — 구현됨: 런마다 성적(도달 레벨·시대·정복)만큼 누적 메타 경험치가
  쌓여 플레이어 레벨이 오르고, 언락(프리셋·카드·리롤)이 레벨 티어로 열린다(`meta.ts`). 런 종료 시 진척도
  화면(`ui/levelUpScreen.ts`)이 경험치바를 채우며 레벨업·해금을 하이라이트. 리롤=레벨 2, 바다=3·하늘=5·
  원거리=7·독=9·초음파=12(`UNLOCK_TIERS`). 경험치 곡선(`META_LEVEL_BASE/STEP`)·적립량(`XP_PER_*`)이 폰에서
  "탕탕탕" 느낌과 해금 페이스에 맞는지 체감 후 조정. `?dev` 패널의 Lv 버튼·"진척도+120"으로 바로 테스트.
  주의: 옛 저장본(bestLevel/runsCompleted)은 metaXp 0으로 초기화됨(디버그로 세팅 가능).
- [ ] **개체별 진화 — 1차 프로토타입 완료, 튜닝·심화 필요**. 새끼가 종 기준선이 아니라 **부모 게놈을 상속 +
  연속 형질 ±변이**(독립 `mutRng`), 카드는 살아있는 무리 전체+기준선에 적용(플레이어가 방향을 쥔다).
  능력형·식성은 변이 제외. `SIM.mutationStrength`=1.5(현재). 남은 일:
  - **변이가 폰에서 눈에 보이는지**(현재 강도 1.5는 개체차가 미묘 — 균일 50 시작 시 t1500에 대사 45~49 수준).
    더 크게 하려면 강도↑ 필요한데, 소수 개체+통과기준 3이라 절대 통과기준 테스트(특히 한파)가 노이즈로
    흔들린다 → 그 테스트들을 **방향 기반(hi>lo)**으로 견고화한 뒤 강도를 올리는 게 정공법(known_issues).
  - **선택압 가시화** — 추운 맵/더운 맵에서 세대가 지나며 대사가 실제로 갈리는지 폰/프로브로 확인·연출.
  - 못 버티는 개체가 솎이는 게 화면에서 읽히는지(연출), 개체 즐겨찾기/계보 등 애착 심화는 그 다음.
- [ ] **날개(비행) 폰 튠** — 비행 대사 대가(flyMetabolismCost 0.25)·시야 보너스(flyVisionBonus 0.5)·
  고산 먹이(mountainFoodPatches 30) 체감 후 조정. 날개 스프라이트가 보이는지, 산 위 흰빛 고산 먹이가
  보이는지, 비행 vs 지상 트레이드오프가 재미있는지. 하늘 개척자 프리셋/날개·튼튼한 날개 카드로 테스트.
- [ ] **야생 고산 종** — 산 위에 사는 비행 야생종(바다 풀뜯이·물고기 떼가 바다에 생명을 주듯 산에도).
  단 `WILD_ARCHETYPES` 추가는 `generateWildSpecies`의 rng 스트림을 늘려 밸런스가 통째로 이동 → kin 처럼
  **독립 rng 스폰**으로 넣어야 한다(별도 설계). 넣으면 밸런스 프로브로 기존 분포 보존 확인.
- [ ] **전투 형질(방어독·원거리) 폰 튠** — 방어독 강도(venomOnHit 100·tickDamage 15 — 잡아먹으면 손해)·
  원거리 사거리(rangedBonus 22) 체감 후. 독 살갗으로 포식이 막히는지, 원거리 사냥꾼이 멀리서 치는지,
  독 걸린 포식자 보라 tint. 방어독이 너무 세면(포식 완전 회피) 강도↓.
- [ ] **산 보물 폰 튠** — mountainFoodPatches 10·treasureSpawn 3·재생 4배 체감 후. 보물 먹으면 동족이
  확 불어나는 대박이 재밌는지. 레벨업으로 바꿀지(현재 동족 증식). 야생 고산 종과 보물 경쟁도 여지.
- [ ] **야생 진화 심화 폰 튠** — 압력별 적응(포식→속도·무리, 먹이부족→저대사, 추위→고대사) 강도 체감 후.
  한 판(≈6000틱) 지켜보면 야생종 형질·스프라이트가 눈에 띄게 갈리는지(작은 풀벌레가 빨라지고 뭉치는지,
  추운 맵서 대사 오르는지). 약하면 wildAdaptRate/Range↑, 너무 세 밸런스 흔들리면↓. 프로브로 분포 먼저.
- [ ] **물고기 학교 + 독 중독 표식 폰 튠** — ① 시작 물고기 15가 학교로 뭉쳐 보이는지·붐버스트가 자연스러운지
  (심하게 사라지면 deepFoodPatches↑). ② 깊은 바다(진한 남청) 먹이가 얕은 청록과 구분되는지. ③ 독 살갗
  프리셋으로 포식자에 보라 오라·방울(중독)이 뜨는지·잘 보이는지. 육지 떼종(작은 풀벌레·들풀) 실체화도 여지.
- [ ] **초음파 폰 튠** — echoBase 130·카드 강도(echo+42 vision-24) 체감 후. 초음파 원(보라)이 잘 보이는지,
  눈 vs 귀 트레이드오프가 재미있는지. 초음파 야생종(두더지·박쥐형) 추가도 여지.
- [ ] **레벨업 속도(xpBase) 튠** — 먹이 경험치 → 레벨업이 폰에서 너무 빠른지/느린지 체감 후 조정
  (`game/config.ts` xpBase 24·xpPerLevel 14). headless 로 750틱 내 종 먹이 섭취율 프로브 가능하나 폰이 정확.

## 시련 실재화 (사용자 방향: 시련이 맵·개체·형질과 겉돈다 → 실재화)
> 2026-07-02 개체형 떼 5종 완료(swarm·raider·isolation·stalker는 실제 떼 개체, chaser 는 단일).
> 독안개만 진짜 전역 재난이라 화면 틴트 유지. 남은 후보는 아래.
- [ ] **외톨이 사냥꾼 카운터 밸런스** — 무리성 카운터가 개체 시뮬에서 약함(무리성의 성장 부작용과
  상충, 단일 시드 노이즈). 폰 체감 후 저항/위협 조정. 필요하면 "이웃 수 기반"을 다시 시도(정교화).
- [ ] **험지 감속 강도 튠** — 소수 개체+국소 보금자리라 감속이 세면 내 종 붕괴 → 온건하게(최대 15%)
  둠. 폰 체감상 효과가 약하면(속도 형질 이점 미미) 조금 올리거나, 험지가 내 종 보금자리를 덜 덮게
  스폰 회피. 수풀 감쇠 강도도 폰 체감 후 조정.
- [ ] **지형×형질 더 넓히기** — 수풀(시야)·험지(속도) 완료(0628623·0177f45). 늪(감속+먹이)·개활지
  (시야 보너스) 등 추가 여지. 산은 현재 완전 차단 → "느리게 통과"로 바꾸면 길찾기 대공사(신중히).
- [ ] **개체형 떼 시련 용어** — 이제 5종이 killRadius>0 이라 다 "보스"로 분류("보스 · 약탈자 무리"
  어색). 단일 강력=보스 / 떼=? 로 용어 재정리 검토(폰 피드백 후).

## 생태·형질 깊이 (이월)
- [ ] 포식자-피식자 수 붐-버스트 안정화
- [ ] 순수 육식(diet>0.7) 자생력 — 현재 잡식 경유가 정답(글래스캐논). 재미 점검
- [ ] herding → boids(정렬·응집·분리) 본격화
- [ ] 환경 hazard(위험) 구역 추가

## 바다 맵 (이월)
- [ ] 물 전용 플레이어 본격화 — 바다 넓게 + 바다 먹이 풍부 + 바다 전용 특성. 보정 코드는 world.ts 대기.
- [ ] 바다 풀뜯이 등 수생 종 생물 도감(`ui/glossary.ts`) 반영

## 그래픽 (이월)
- [ ] 생물 애니메이션(꿈틀/지느러미), 죽음/번식 연출
- [ ] 회전 피벗이 몸 중심과 약간 어긋남 — 정렬 보정
- [ ] 더 정교한 아트(현재 makeCreatureTexture 절차 생성)

## 성능
- [ ] 렌더 Graphics 재드로우 → ParticleContainer/스프라이트 (소수 개체 전환으로 우선순위 낮아짐)

## 아이디어 보관 (당장 아님)
- 세대 계보(부모-자식 추적) 애착 심화 — "복잡하다"고 보류(사용자). (개체 즐겨찾기는 2026-07-08 구현됨: 단골 별 마커)
- 게놈 → 위협 요약 자동 생성 (보스 예고로 부분 달성 — bossPreview/bossCounter 가 토대)
