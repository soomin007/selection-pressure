# 백로그 (단기 후보)

> 앞으로 할 일만 적는다. 끝낸 항목은 지우고, 무엇을 했는지는 `session_logs/` 에 남긴다.
> 폰 검증 대기 항목은 여기 말고 최신 세션 로그의 "미해결/다음 할 일"에 있다.
> **형질/시스템 큰 로드맵(초음파·날개/지느러미/다리·독침/원거리 등)은 [`../ROADMAP.md`](../ROADMAP.md)가
> 단일 소스다. 여긴 그 로드맵에서 다음에 착수할 것 + 자잘한 튠만 끌어온다.**

## ★★★★★ 다음 세션 착수 순서 (2026-08-12 밤 2차 갱신)

> **어디까지 왔나:** 카드 재설계 2차 → TTK 배속 기준 재조정 + 행동 분화 넷(08-11) → 몸싸움의
> 리듬(치고 빠지기 · 08-12) → 폰 제보 빠른 수정 둘(모바일 화면 · 시험 원 잔류) → **순종 처방 셋
> 구현 완료**(08-12 밤 · 순종1s 17.0 → 71.9% · 도착 40.1 → 56.0% · 먹이 회복 옛 세계 동률 85%).
> 폰 실기 1차 판정: **포식자 사냥 합격 · 보스 리듬 불합격 · 첫 시대 날먹 · 모으기 시험
> 좌절 · 빌드 다양성 부재**(**[사용자 2026-08-12]** · 상세는 아래 각 항목).
>
> **다음 세션 추천 순서:**
> 1. **폰 실기 판정(순종 처방 셋)** · "애들이 말을 잘 안 들어"가 실제로 풀렸는지는 손만 안다.
>    같이 볼 것: 행군 중 스치며 먹는 것이 자연스러운가 · 도착한 무리가 자리를 지키고 서는 것이
>    「말 듣는다」로 읽히는가 「굳었다」로 읽히는가 · 모으기 시험이 이제 할 만한가(시간·마리 수
>    상수는 일부러 안 건드렸다 · 순종이 풀리면 그 둘은 다시 재야 하는 값이라서).
> 2. **1.7 · 빌드 다양성** — 설계 합의가 먼저인 대화 작업(초식 트리 · 재해 대응 빌드).
> 3. **2번 · 성장·난이도 재측정** — "이빨 2단 = 최상위 포식자" 날먹 체감의 답. 전투가 세 번
>    바뀌어 어차피 전면 재측정이다(보스 격퇴 시간 포함). ⚠ 순종 처방이 조종 판의 채집·경험치
>    흐름을 움직였으므로(스침 채집) 조종 밸런스는 여기서 함께 재라.
> 4. (작게) 보스전 리듬 가독 — 리듬 폭 또는 렌더 연출(1번 꼬리 참조).

### 1. 행동 분화 1·2차가 남긴 꼬리 (폰 실기 뒤에)

- [ ] **매복 잠복이 chooseGoal 의 먹이 탐색을 안 막는다(v1 한계).** 잠복은 배회 가지에서만 발동해,
  기운이 넉넉해도 먹이가 보이면 그쪽으로 걸어간다. "숨어 기다리다 덮친다"가 온전하려면 잠복 중
  표적 탐색 반경·의욕을 함께 낮춰야 한다 — 프로브 A/B 와 한 몸.
- [ ] **잠행·치고 빠지기가 폰에서 읽히는가.** 잠행(0.62배)→돌진 전환, 그리고 몸싸움의
  간 보기→대시→몸부림(2026-08-12)이 배속 화면에서 「실랑이」로 보이는지 실기 판정.
  안 읽히면 잠행 자세(몸 낮춤·투명도)나 물기 순간 연출 강화를 렌더에 얹는 것이 다음 수다.
- [ ] **보스전 전사의 들락날락(반격 리듬)이 아직 안 읽힌다** — 폰 실기 판정 결과
  **[사용자 2026-08-12]** "포식자 사냥은 잘 구현된 것 같아. 보스는 아직 별 다를 게 없어보이지만".
  sim 은 들어갔으니 다음 수는 ① 리듬 폭 확대(raidRecoil 값) ② 렌더 연출(반격 순간·물러남 잔상).
  ⚠ 리듬 폭을 키우면 격퇴가 늦어진다 — **이제 자가 있다**: raid 의 「격퇴초」 열(2026-08-12 신설 ·
  시대 0 = 6.1~6.6초 · 시대 2 = 17.9~21.8초/35초 한도). 폭을 키우고 이 열이 한도를 위협하는지 보라.
- [ ] **위험 기억이 초식 생존율을 실제로 올리는가** 프로브로 재기(지금은 행동만 넣고 효과 크기 미측정).

### 1.5 카드 재설계 2차가 남긴 자잘한 것들 (코드 반나절 이하 · 폰 실기 뒤에)

- [ ] **예비 카드 4장**(심판 탈락 · 다음 기수 1순위): 숨통을 무는 법(이빨 처형 · cull 과 양자택일이었다) ·
  거북의 시간(수명 삭제 · undying 과 결 중복) · 마지막 잔치(죽음 유산 · salmonrun 과 계열 중복) ·
  호저의 가시(반사 · ratel/zebrakick 에 밀림). 정의는 세션 로그의 심판 결과 참조.
- [ ] **「등에 그린 눈」의 대가가 숨기 없는 종에게 무효다**(감지 상한이 시야 반경이라 1.3배가 그 밖을
  못 넘는다 · 테스트 에이전트 발견). 문구는 「숨은 몸도」로 좁혀 정직하게 해 뒀지만, 대가가 실질이
  되려면 숨기·수풀·웅크림과의 상호작용을 프로브로 재고 필요하면 다른 대가로 갈아야 한다.
- [ ] **「뱀의 응시」 A↔B 교대 재발동**(히스테리시스가 표적을 오가면 동결이 거의 상시가 된다 ·
  검증 지적). 프로브로 동결 빈도를 재고 과하면 재발동 쿨타임을 넣는다.
- [ ] **사체 렌더가 뼈 세 줄뿐이다** · 폰에서 읽히는지 실기 확인. 「곧 삭는다」(마지막 4분의 1 페이드)도.
- [ ] 내 종 패널(buildPanel)은 가진 고유 카드의 **대가 줄을 아직 안 보여 준다**(드래프트·구입 화면·
  대백과는 보여 준다). 패널이 길어지는 것과 맞바꿈이라 폰 실기 뒤 판단.

### 1.7 빌드 다양성 — 초식 강화 트리 · 재해(한파/독 안개/역병) 대응 빌드가 없다 (**[사용자 2026-08-12]**)

> 원문: "지금 상황에서는 **초식을 강화하는 트리나, 한파/독 안개/역병 이런 걸 피하는 빌드가 없어서
> 다양성이 살아나지 못하는** 느낌이야. 있는 빌드들은 양방향이라고는 하지만 사실상 한 방향으로만
> 성장할 수 있으니까."
>
> 설계 작업이라 합의부터(방향만 받고 임의로 채우지 말 것). 생각할 축:
> - 초식 강화: 가죽·무리 기둥이 있지만 "굶주림을 안 풀어 준다"(아래 느린 거인 절과 같은 뿌리).
>   먹이 효율·군집 시너지·재생 같은 **초식만의 공격적 성장**이 없다.
> - 재해 대응: 한파/독 안개/역병은 형질 필터인데 **그 필터를 향해 미리 빌드할 수단**(내성 카드·
>   기둥)이 없다 → 재해가 "선택"이 아니라 "복권"이 된다. 고유 카드 중 열병의 흉터·산 같은 몸이
>   드물게 닿는 정도.
> - 예고와 묶으면 강해진다: 다음 시대 위협 예고를 보고 대응 빌드를 사는 루프(전염병 주식회사의
>   치료제 타이머와 같은 자리).

### 2. 성장 속도와 난이도를 처음부터 다시 잰다 (행동 분화와 별개로 진행 가능)

> **2026-08-12 재측정 1차가 끝났다**(자 수리 후 · 상세는 `session_logs/2026-08-12.md` 세션 4).
> 새 기준선(전부 지시 없음 하한선): 정복 3/80(전부 사냥꾼) · 도달 시대 3.3~4.6 · 시대 3+ 사인은
> 폭염 중심 · 판당 방울 33.6~61.3(가격표 한 기둥 4단 = 20 대비 크게 넘침) · 판당 카드 14.4~17.2장 ·
> raid 격퇴초 시대 0 = 6.1~6.6초(무료) · 시대 2 = 17.9~21.8초 · 몰기(수리된 --drive)는 카운터 성립
> 조합을 7~8/8 로 올리고 불가 조합에선 무리를 전멸시킨다.
> **「이빨 2단 날먹」 기전도 실측 확정**: 야생 공격 천장이 전 시대 64 로 고정이라 이빨 2단(공격 78 ·
> 방울 8개)이 시대 0~2 야생 전부를 먹잇감으로 뒤집는다(잡아먹힘 194~206 → 0~1). 레버 후보 넷과
> 함께 세션 로그에 있다 · **야생 능치가 걸린 결정이라 사용자 합의가 먼저다.**
> 남은 것: ① 구입 정책 자(성장 속도를 재는 자 · 임의로 만들지 말 것) ② 값 결정(폰 체감이 기준점).

> **폰 실기 실측 둘(2026-08-12 · 값을 정할 때 이 체감이 기준점이다):**
> - **[사용자 2026-08-12]** "초기화 후 첫 시대가 너무 날먹인 느낌 · **이빨을 두 단계만 찍어둬도
>   사실상 최상위 포식자가 돼서 위협이 되는 게 없어**" → 첫 시대 야생의 이빨/방어 천장과
>   방울 가격표(이빨 2단 도달 시점)를 함께 봐야 한다.
> - **[사용자 2026-08-12]** 모으기(자리 지키기) 시험이 너무 어렵다: "시간도 촉박하고, 모아야 하는
>   마리 수도 많고, 결정적으로 **애들이 말을 잘 안 들어**" → 앞 둘은 trial 상수(시간·holdTarget
>   = pop×0.5, 상한 12)의 문제고, 뒤는 5번(순종 처방 셋)의 문제였다. **5번은 풀렸다(2026-08-12 ·
>   순종1s 17.0 → 71.9%)** → 이제 trial 상수를 **새 순종률 위에서** 다시 재라(옛 체감으로 낮추지
>   말 것 · 말을 듣게 된 무리에게는 지금 값이 알맞을 수도 있다).
>   **+ 조기 합격 확정이 들어갔다(2026-08-12 밤 · [사용자] 제안)**: 목표에 닿는 순간 합격이 잠기고
>   「자리 지키기」는 유지가 필요 없어졌다(「무리」 시험만 끝값 판정 유지 · 사유는 game.ts 주석).
>   trial 상수 재측정은 이 규칙 위에서 한다.
>
> ⚠⚠ **전투 상수가 두 번 연달아 바뀌었다** — 이 항목이 훨씬 커졌다.
> 2026-08-10: 간격 3 → 10 · bias 0.34 → 0.08 · scale 1.5 → 0.5 · 피해 18 → 25 (사냥 0.17초 → 1.05초).
> **2026-08-11: 간격 10 → 18 · bias 0.03 · scale 0.3 · 피해 25 → 10** (실측 실랑이 2.4~3.0초 ·
> [사용자] 목표 「1배속 2.5~3초」). 여기에 행동 분화(잠행·위험 기억·지그재그)까지 겹쳤다.
> 그래서 **사냥 효율·포식 압력·개체 수·보스 격퇴 속도가 전부 두 번 이동했다.**
> · 08-10 실측(시대 4 도달 9/30 → 14/30)도 이제 **한 세대 전 세계의 것**이다 — 인용하지 마라.
> · **보스 격퇴가 특히 의심 지점이다**: 물기 간격 18은 전사 개인의 초당 피해를 08-10 대비 절반
>   아래로 내렸다(격퇴는 반격 쿨타임이 따로 있지만 물기 쪽 기여가 줄었다). 격퇴 시간 재측정 필수.
> ⚠ 옛 밸런스 수치(도달 시대·개체 수·보스 통과율)는 **전투 이전 세계의 것**이라 인용하지 마라.
카드가 도장을 안 주므로 **옛 밸런스 수치가 전부 무효다.** 특히:
- `src/sim/tiers.ts` 의 `TIER_STEPS` 실측표 — 「카드 풀 90장이 도장을 준다」 전제라 통째로 무효
  (그 자리에 무효 표시를 해 뒀다).
- **시대 보상이 지금 비어 있다.** v8 에서는 뽑은 카드의 도장을 ×2.0~4.9 로 곱해 줬는데(강화 ×N),
  곱할 도장이 없어져 **지금은 그냥 카드 한 장을 더 고르는 기회**다. 시대를 넘은 보상이 그만큼 얇아졌다.
  → **방울 보상으로 옮기는 것이 맞다**(성장이 방울 한 갈래로 들어오므로). 값은 여기서 함께 정한다.
- 손잡이: 방울 수입(`GENE_AWARD` · `POP_MILESTONES` · `geneCrisisMinPeak`) · 가격표(`TIER_STEPS`) ·
  레벨업 곡선(`GAME.xpBase` · `xpPerLevel` · `leadStageXpCap`) · 시험 문턱(`trialRefPop` · `trialScaleCap`).
- ⚠ 재는 자는 `probe replay` 다(v9 에서도 자가 검사 통과 확인). 세계를 따로 만들어 재던 모드
  (`extinction` 등)의 수치는 **근거로 쓰지 마라**.
- ⚠ **먼저 답할 질문 둘** (2026-08-08 에 「판 분석 기능이 있어야 정할 수 있다」며 미뤄 둔 것 ·
  이제 `probe replay` 로 답할 수 있다):
  · **한 판에 카드를 몇 장 받게 할 것인가.** 지금은 레벨업마다 받아 10장쯤이다. 카드가 성장을 안 주는
    지금 **레벨업의 뜻 자체가 달라졌으므로**(성장이 아니라 판의 색이 하나 붙는다) 함께 정해야 한다.
  · **「한 판에 방울 25.9개」로 한 기둥 4단(16개)이 겨우 선다.** 그것이 너무 느린가 알맞은가.
  실측 없이 값부터 만지지 마라 — 2026-08-08 세션에서 내 추측이 **네 번 중 네 번 틀렸다.**

### 3. 카드 효과가 「지금 켜졌다」를 화면에서 알아채게 한다
카드 효과에 조건이 붙는 한(1번의 「굶주린 사냥꾼」도 조건부다) **언제 작동하는지 사람이 알 수 없으면
성장이 안 읽힌다**(전달 규칙 위반). `sim/perks.ts` 에 `activePerks(perks, ctx)` 를 만들어 뒀다 —
sim 과 **같은 판정**을 쓰므로 화면과 실제가 갈릴 수 없다. 붙일 자리는 2026-08-10 UI 조사 결과 참고.
⚠ 1번(재설계 2차)이 효과의 모양을 바꾸므로 **그 뒤에** 한다.

### 4. 알파를 없앨지 정한다 (**[사용자 2026-08-09]** "지금 구성에서는 알파가 굳이 필요해보이지 않아")
사용자 판단은 **불필요** 쪽이다. 다만 없애는 순간 **명령이 어디서 나가는가**를 다시 정해야 하므로,
착수 전에 그 한 가지를 합의하고 시작한다. 지금 얽혀 있는 것:
- `world.hearsOrder(x,y)` 가 **알파 위치 기준 반경**으로 「이 개체가 명령을 듣는가」를 판정한다.
- ~~지휘 공백(`leadVacuum`)~~ **이미 걷어냈다(2026-08-10)** · **[사용자]** 「이끌던 개체 어쩌고
  아예 없애줘」로 명령 게이트 둘에서 제거했다. 값은 아직 세지만 **아무도 안 본다** — 알파를
  없애기로 하면 `HERD_VACUUM_TICKS` 와 함께 이 죽은 값도 같이 지운다.
- 승계·`passBaton`·입력(내 개체 탭)·렌더 표식(`worldView.drawLead`)·테스트.
- 후보: ① 무리 무게중심 기준 반경 ② 살아 있는 내 종 전부(반경 없음) ③ 화면에 보이는 개체만.
  ⚠ 어느 쪽이든 **순종률 기준선이 통째로 이동한다**(2026-08-12 처방 뒤 순종1s 71.9%) ·
  프로브 `order` 재측정과 한 몸.
- 사용자 제보 하나 더: **지휘봉이 넘어가도 티가 안 난다** — 이양 순간 나가는 것이 이동 명령과
  똑같은 "go" 핑 하나뿐이다. 남기기로 하면 이것부터 고친다.

### 5. ~~명령이 먹히게 한다 · 처방 셋~~ **구현 완료(2026-08-12 밤)** · 남은 것은 폰 실기와 꼬리 셋
구현·실측은 `session_logs/2026-08-12.md` 세션 3. 순종1s 17.0 → **71.9%** · 도착 40.1 → **56.0%** ·
먹이 회복 옛 세계 동률(85%). 남은 꼬리:
- [ ] **티어 0 목소리 520 이 첫 시대(한 화면 세계)를 거의 다 덮는다** → 첫 시대에서 무리 티어의
  목소리 이득이 손에 안 잡힌다. 큰 세계(후기 시대)에서만 읽히는 것이 의도인지 폰 실기로 판정.
- [ ] **`HERD_VOICE` 1단 700 은 임의값이다**(단조 증가 유지용 내 판단 · 520~900 기하 중간).
  2번 재측정 때 함께 재라.
- [ ] **스침 채집 반경 30 은 「옛 세계 채집 동률」로 정했다**(9 는 먹이 45%로 붕괴 · 실측 표는
  세션 로그). 성장 재측정에서 조종 판 경험치 흐름이 이상하면 이 값이 첫 레버다.
⚠ 「이동 편향」은 실측에서 폐기됐다(절반 도달 36/72 → 9/72) · 다시 꺼내지 마라.
⚠ 4번에서 알파를 없애기로 하면 목소리 반경 구조는 **다시 설계**다(순종 기준선도 다시 잰다).

### 6. 「잡아라」와 나머지 휠 명령을 제대로 만든다
`order.kind` 를 sim 이 읽게 하는 작업. 여덟 칸 중 여섯이 지금 `ready:false` 로 잠겨 있다.
⚠ 표적 지정 경로(`lead.orderTargetId` → `behavior.leadBiteTarget`)는 **sim 에 이미 있는데 호출부가
0건**이다 · 거기서 시작하면 된다. ⚠ 표적 선택이 바뀌면 명령을 준 세계의 rng 스트림이 밀린다
(명령 없는 세계는 안전) · 별도 커밋으로 떼고 프로브로 확인하라.

### 손에 익혀 둘 도구 (2026-08-09 신설 · 내일 바로 쓴다)
- `node scripts/balance-probe.mjs replay --code=SP1-…` — 사람 판을 그대로 되살려 기록과 한 줄씩 대조.
  어긋나는 첫 줄이 곧 이분 지점이다.
- `node scripts/balance-probe.mjs replay --selftest [--hands]` — **재생기 자신을 검사한다.**
  손 붙인 판 5/5 시드 완전 일치가 지금 기준선이다. 여기가 깨지면 다른 수치를 믿지 마라.
- `node scripts/balance-probe.mjs extinction --pips=… --keys=… --warmup=3540` — 재난 넷을 같은 자로.
  ⚠ 세계를 따로 만드는 모드라 **검증 안 됨** · replay 로 옮겨야 한다.
- ⚠ `npm run …` 은 이 체크아웃에서 안 돈다(`node_modules/.bin` 부재) · 진입점을 직접 부른다:
  `node node_modules/typescript/bin/tsc --noEmit` · `node node_modules/vitest/vitest.mjs run` ·
  `OVERLAP_PORT=5188 node scripts/overlap-check.mjs`.

## 카드 재설계에서 **철회된 안 둘** (다시 꺼내지 마라)

> 설계안 문서(`card_redesign_proposal.md`)는 스스로 적어 둔 대로 구현이 끝나 지웠다(2026-08-10).
> 그 문서에만 있던 **「다시 꺼내지 마라」 둘**만 여기로 옮긴다. 나머지 내용(v8 진단·비용 표)은
> 이미 `session_logs/2026-08-08.md` 와 `src/game/cards.ts`·`src/sim/perks.ts` 머리 주석에 있다.

- **「카드를 아예 없애고 방울만 남기는 안」은 철회됐다.** 드래프트는 이 게임의 재미 축이고,
  그 제안은 **[사용자 2026-08-07]** 의 「카드에서 도장을 뺀다」를 내가 과하게 확대 해석한 것이었다.
  **[사용자 2026-08-08]** 이 「카드는 유지하고 도장만 뺀다」로 못 박았다.
- **「카드를 열쇠 20~30장으로 통일하는 안」도 철회됐다.** 한 종은 열쇠를 **3개까지만** 가지므로
  (`MAX_KEYS`) 한 판에 의미 있는 카드가 3장뿐이 된다. 한 판에 카드를 10장쯤 받으니 **나머지 일곱 장이
  줄 것이 없어진다.** 그래서 카드의 대부분은 조건부 특성이어야 한다.

## ★★★ 2026-08-09 검증이 남긴 것

> 커밋 6d8c079 의 「남은 검증」을 돌려 나온 것들이다. 결정론 · 겹침(41화면) · 구입 화면 단계 복귀
> (신규 21건)는 통과했고, 아래는 **드러났지만 그 커밋의 범위 밖이라 안 고친 것**이다.
> 증상·원인·실측은 [`known_issues.md`](known_issues.md) 의 2026-08-09 항목 넷에 있다.

- [x] ~~`achievements.ts` 의 `unlockedCache` 오염~~ **수리 완료(2026-08-12 · 97d3743).**
  `setSavedProgress` 가 캐시까지 지운다 · 교차 확인 통과 · **옛 econ·growth 수치는 전부 기준선
  자격을 잃었다**(새 기준선은 session_logs/2026-08-12.md 세션 4).

- [ ] **「가라」의 목표 자체에 길이 없을 때의 직진 폴백.** `navTo` 는 `findPath` 가 빈 배열을 돌려주면
  목표로 직진한다 · **방울 도달성 수정으로는 안 풀린다**(그건 목표를 고르는 자리를 고친 것이고, 이건
  사람이 목표를 놓는 자리다). 노출이 압도적으로 크다: 못 가는 목표에 붙들린 개체틱이 군도 166 대 72,
  대양 816 대 6 이다. 포기 카운터가 첫 수다.

- [x] ~~「피해라」가 맵 가장자리·구석에서 24% 약하다~~ **수리 완료(2026-08-12 · dd34666).**
  `fleeClear` 에 세계 경계 검사 · 회귀 테스트 2건(무력화 시 빨간불 확인) · 골든 재기준선 ·
  생태 A/B 붕괴 없음. 폰에서 가장자리 「피해라」 손맛만 남았다.

- [ ] **휠 여덟 칸 중 여섯이 「아직 준비 중입니다」가 되면서 성장 신호가 죽었다.** `commandWheel.ts` 가
  스스로 적어 둔 목적 ②(회색 칸이 성장을 손에서 알린다)가 여섯 칸에서 사라졌다 · 옛 힌트("이빨 1단이
  되면 열립니다")가 함께 지워졌기 때문이다. 기본 프리셋에서 실제로 쓸 수 있는 칸은 「가라」 하나뿐인데
  런 시작에 **7/8 이 회색인 휠**이 뜬다(`open` 의 `items.length <= 1` 가드는 잠긴 칸까지 세므로 안 걸린다).
  위 착수 순서 4번(「잡아라」와 나머지 휠 명령)에서 풀릴 문제지만, 그전까지 **무엇이 자라고 있는지**를 다른
  자리에서 보여 줄지 판단이 필요하다.

- [ ] **산 직후 2.2초는 다시 안 멈춘다.** 구입 성공 → `genePanel.close()`(phase=watch) → `playTierUps()` →
  `moment.apex` 가 z18 전체 화면으로 2200ms 덮는데 **그동안 세계가 정상 속도로 돈다.** 방금 구입 화면에
  「고르는 동안 시간이 멈춰 있습니다」라 적어 둔 참이라 바로 뒤에서 어긋난다 · **[사용자 2026-08-09]**
  제보("보다보니 멸종해버렸는데")의 축소판이 승급 연출 자리에 그대로 남아 있는 셈이다. 게다가
  `apexRoot` 는 `pointer-events:none` 이라 그 2.2초의 탭이 캔버스로 새어 **명령이 나가기도 한다**.

- [ ] **`shop` 단계에서 HUD 다섯 개가 함께 숨는다.** 멈춤을 `phase` 재사용으로 만든 결과
  `phase === "watch"` 를 보는 다섯 곳(명령 한 줄 · 목표 줄 · 형질 패널 · 조종 안내 · 미니맵)이 동시에
  꺼진다. 지금은 전체 화면 딤 뒤라 무해하지만, **구입 화면을 바텀 시트로 바꾸면 그 순간 결함이 된다**
  (구입 화면을 여는 유일한 문인 목표 줄의 방울 카운터도 함께 사라진다).

- [ ] **`openGeneShop` 이 `paused` 를 안 본다**(형제 함수 `setHerdOrder`·`passBaton` 은 본다).
  지금 UI 로는 못 닿고 상태 손상도 없다(실측). **게이트가 갈린 것만 기록해 둔다.**

## ★★★ 지형·명령 수정 뒤에 남은 것 (2026-08-08 · 오목한 만 수정에서 함께 드러남)

> 「가라」가 오목한 만에 못 들어가던 것은 고쳤다(72e3598). 그때 함께 드러났으나 **일부러 안 고친 것**들이다.
> 근거와 실측은 [`known_issues.md`](known_issues.md) 의 「도착했나를 직선거리로 물으면」 항목.

- [ ] **`lineOfSight` 를 4연결로 통일할지 (보류 · 승인 필요).** 지금은 이동·`findPath` 가 4연결인데
  이것만 8연결이라 「직선으로 보이는데 직진으로는 못 가는 자리」가 생긴다. `walkableLine` 을 따로 만들어
  급한 자리는 막았지만 **근본은 둘로 갈린 통행 모델**이다. ⚠ 바꾸면 먹이 길찾기·무리 뭉침·보스가 함께
  보는 판정이 달라져 **배회 분기 진입이 바뀌고 `world.rng` 소비가 밀린다** = 야생 스폰·진화·보스
  밸런스가 통째로 이동(econ 수치 전부 재측정). 바꾼다면 그것만 하는 커밋으로, 프로브 전면 재측정과 함께.
- [ ] **명령 수락 게이트가 길의 「길이」를 안 본다.** `herdCanReach` 는 경로 존재만 확인한다. 우회로가
  목소리가 닿는 거리(티어 0 520px · 2026-08-12 처방 ①)의 몇 배여도 「갈 수 있다」로 통과해, 선두가
  사정거리를 벗어나면 명령을 잃고 되돌아온다. 지킬 수 없는 약속의 나머지 절반이다(`bossEligible` 이
  도달 가능성을 안 보는 것과 같은 계열).
- [ ] **지형 생성기에 정리 패스가 없다.** 호수·강 전용 코드 없이 표고 분위수로만 물을 자르고, 생성 후
  고립 육지 제거·1칸 목 메우기·대각 핀치 정리를 **하나도 안 한다.** 그래서 육상 종이 원리적으로 못 가는
  육지 포켓이 대륙에서 평균 4칸 생긴다(진도 2 맵 기준 · 재현 구현 추정). ⚠ 타일 구성을 바꾸면 먹이
  배치가 바뀌어 밸런스 기준선이 흔들리므로, 손댄다면 프로브 재측정과 한 몸으로.

## ★★★★ 방울(유전자 점수) 1단계가 배포됐다 · 이제 폰이 판정할 차례 (2026-08-08)

> 구현·검증은 끝났다(결정론 세 겹 통과 · 553/553 · 겹침 37화면). 경위는
> [`session_logs/2026-08-08.md`](../../session_logs/2026-08-08.md). **남은 것은 코드가 아니라 손이다.**

- [ ] **「방울 주우러 가는 게 재밌는가」 · 이 설계 전체가 그 전제 위에 서 있다.** 폰 1x 배속 실기.
  · 금빛 방울이 눈에 띄는가 · 화면 밖 방향 쐐기와 미니맵 점으로 어디 있는지 잡히는가.
  · 무리를 몰아 밟게 하는 것이 **조작할 맛**인가, 아니면 심부름인가.
  · **회수율 83.8%**(판당 25.9개 중 4.6개가 필드에 남는다)가 답답함으로 읽히는지 목표로 읽히는지.
    설계대로 「걸어가 밟아야 주워진다」의 결과라 결함은 아니지만, 체감은 코드로 판정할 수 없다.
  · 구입 화면을 **라운드 도중에 열지 라운드 사이에만 열지**도 실기로만 정해진다(미정).
- [ ] **위기 회복이 다섯 판에 한 번꼴(0.20회)이 됐다.** 시대 전환마다 터지던 가짜를 걷어내니 진짜는
  원래 이만큼 드물었다. 화면이 안내하는 다섯 출처 중 하나가 **사실상 거의 안 도는** 상태다.
  값(5)이 가장 큰 것과 짝은 맞지만, 있으나 마나 하면 안내가 공허하다.
  ⚠ **만지기 전에 폰 체감부터.** 값을 올리거나 문턱을 낮추는 것은 「접으려는 사람을 돕는」 은근한 보정과
  같은 자리라, 세게 걸면 티가 난다. 고친다면 `GAME.geneCrisisMinPeak`(20)이 첫 레버다.
- [ ] **판당 수입이 25.9 로 확정됐다**(설계 모델 30 대비 −14%). 가격표(`TIER_STEPS`)는 그대로 유효하다
  (한 기둥 20 을 세우고 다른 범주 1단 3까지 닿는다). **수입이 준 것은 밸런스 변경이 아니라 가짜를 뺀
  결과**이므로, 여기서 값을 올리면 그 가짜를 다시 넣는 셈이다. 조종이 붙으면 더 나온다(이건 하한선).

## ★★★ 느린 거인만 카드 예산이 79% 로 남았다 (2026-08-07 · 앞의 「1순위」는 전제가 틀렸다)

> ⚠ **먼저 정정.** 여기 있던 「사냥꾼 3.0 vs 초식 1.8 · 카드 12.0 vs 6.3」 은 **잘못된 자로 잰
> 값이었다.** 프로브의 시드 풀이 16개뿐이라 `--seeds=24/48` 이 조용히 16으로 잘렸고, 16시드에서
> 도달 시대는 ±0.4 씩 그냥 흔들린다. 96시드 대응표본으로 다시 재니 격차는 **0.36 ±0.26** 이었다
> (주장된 1.2 가 아니다). 「사냥꾼 3.0 · 정찰자 3.0」 은 어떤 시드 수로도 재현되지 않았고,
> 「초식 둘이 나란히 1.8」 도 실제로는 1.8 / 2.2 였다. 2대2 구도는 측정된 적이 없다.
> **진짜였던 축은 도달 시대가 아니라 카드 예산이다** (8.8 · 8.4 vs 11.1).
> 경위: `session_logs/2026-08-07.md`. 자는 고쳤다(시드 풀 96 + 잘릴 때 경고).
>
> ⚠ **v9(2026-08-10) 이후 이 절의 잣대가 갈렸다.** 「카드 예산」은 v8 에서 **곧 성장의 크기**였지만
> (카드가 도장을 줬으니까), 지금 카드는 도장을 안 준다 ▸ 카드 장수는 성장이 아니라 **판의 색이 몇 겹인가**다.
> 갈래별 **성장** 격차는 이제 **방울 수입**으로 재야 하고, 카드 장수 격차는 「고를 거리가 얼마나 있었나」로
> 읽어야 한다. 아래 수치는 전부 v8 세계의 것이다 · 목표나 기준선으로 인용하지 마라.

2026-08-07 에 고친 것: **사냥 경험치 빚 버그**(시대 전환마다 −85~−2410 · 다섯 프리셋 전부 카드
+1.0~+1.5) · **다산 초식 부 범주 가죽 → 눈**(도달 2.25 → 2.63 · 카드 8.8 → 11.6 = 사냥꾼의 96%).

- [ ] **느린 거인만 남았다.** 카드 9.6 = 사냥꾼(12.1)의 79%. **[사용자]** 확정 「초식 거인 경로는
  반드시 만든다」가 걸린 자리다. 지금 도장은 {가죽 4, 무리 3} 인데 **둘 다 굶주림을 안 풀어 준다**
  (무리 1단은 `HERD_GRAZE_SHARE` 로 채집 수입을 −6% 깎기까지 한다). 다산 초식과 같은 결(부 범주
  재배치)이 유력하다. **미검증 추측이니 반드시 96시드 A/B 로 재라.**
  ⚠ **평균만 보고 후반을 만지지 마라.** 거인은 정복 3/48 로 다섯 중 **최다**다. 평균 도달이 낮은 건
  천장이 낮아서가 아니라 초반에 통째로 무너지는 런이 많은 **이봉 분포**이기 때문이다.
- [ ] **자랄수록 후반에 더 굶어 죽는다** (v8 실측: 시대 4 굶주림 40.6 → 56.2 · 시대 5 는 69.4 → 72.7).
  `derivedUpkeep = 1 + 0.038 × Σ티어` 는 그대로다. **성장이 곧 생존이 아니다** · 카드를 +1.2장 더 줬는데
  정복은 안 늘었다(9/240 → 7/240). 눌려 있던 성장 곡선이 풀리며 드러난 **난이도 곡선의 문제**다.
  ⚠ **v9 에서 인과가 끊겼다**: 카드는 티어를 안 올리므로 「카드를 받을수록 유지비가 오른다」는 이제 거짓이다.
  유지비를 미는 것은 **방울로 산 단**뿐이다 → 같은 현상을 **방울 지출 대비**로 다시 재라(위 1번과 한 몸).
- [ ] **`leadStageXpCap: 165` 를 다시 재라.** 지금까지는 음수 gain 이 `stageXp` 를 깎아 상한 여유를
  되돌려 주는 우회로가 있었다(`if (gain > room)` 이 음수일 때 거짓). 버그를 막아 그 우회로가 사라졌으니
  165 는 **누수가 있던 시절에 튠된 값**일 수 있다.
- ⚠ **과거 밸런스 수치는 전부 비교 불가다.** session_logs·backlog 에 적힌 도달 시대·카드 수는
  **경험치 빚이 살아 있던 세계**의 것이다. 목표나 기준선으로 인용하지 마라.

## ★★★★ 황금 고블린 · **구현 완료(2026-08-12 · 902fc73 배포)** · 남은 것은 폰 판정과 값 튠

> 시험 「표시된 것 사냥」 → 「금빛 짐승 잡기」. **[사용자 2026-08-07]** 확정분(생태 밖 특별 개체 ·
> 금색 번쩍 · 도망 · 시험 끝나면 소멸)과 내 판단분(본진 비움이 대가 · 한 마리씩 차례로 · 시간 제한
> 없음 · 도망 속도 = 내 종 비율 · 미니맵 방향 표시) 전부 들어갔다. 격리 계약(야생·rng 1비트 불변)은
> `sim/goblin.test.ts` 가 못 박는다. 잡기가 **접촉**이라 이빨 0단(초식)도 할 수 있게 됐다(옛 이빨
> 게이트 제거). 경위: `session_logs/2026-08-12.md` 세션 6.

- [ ] **폰 판정**: 금빛이 설명 없이 「쫓을 것」으로 읽히는가 · 도망 속도 비율(`GOBLIN.speedRatio`
  0.88 · 내 판단 출발값)이 「몰아야 잡힌다」 손맛인가 · 목표 수(기본 2 · 상한 3)가 16초에 알맞은가.
- [ ] **「사냥」(옛 잡아라) 배선은 이 시험과 별개로 남아 있다**(6번 · orderTargetId 경로 호출부 0건).
  고블린은 접촉 잡기라 이 명령 없이도 되지만, 야생 사냥 조준에는 여전히 필요하다.
- [ ] ⚠ 오늘 낮의 growth 재측정 중 **mark 시험이 낀 열(시험 초과·불씨)** 은 고블린 이전 세계 값이다 ·
  전면 재측정(2번) 때 자동으로 갱신된다.

## ★★★ 은근한 보정에 등급 축을 하나 더

- [ ] **[사용자 2026-08-06]** "후반에도 운이 안 좋으면 칸수 하나짜리 흔한 카드만 만나는 거고
  (물론 여기에도 보정이 조금 들어갈 수도)." 지금 `draftBias` 는 「내 방향 범주」 가중만 올린다 —
  거기에 **「연속으로 작은 카드만 뜨면 큰 등급 확률이 오른다」**를 얹는다.
  경계는 그대로: **등장 확률만 손대고 효과·수치는 불가침** · 결정론 유지 · 프로브에서 끌 수 있게.
  ⚠ v9 읽는 법: 원문의 「칸수 하나짜리」는 도장 +1 카드를 가리키던 말이고 그런 카드는 이제 없다.
  같은 뜻은 **「늘 켜지지만 배수가 작은 흔함 카드」**다. 「내 방향」 판정도 도장이 아니라 특성의 축으로
  바뀌었다(`cardFavorsCategory` · `CATEGORY_AXES` 가 범주 ↔ 축을 잇는다).

## ★★★ 높은 희귀도의 「귀하다」는 느낌 · **v9 에서 처음부터 다시 재야 한다** (**[사용자 2026-08-07]**)

> **[사용자 2026-08-07]** "높은 희귀도 카드가 생각보다 너무 잘 나와서 귀하다는 느낌이 안 들어."
>
> ⚠ **여기 있던 2026-08-07 진단과 처방 셋은 지웠다 · 전부 v8 전제였다.** 주범으로 지목했던
> 「3장 중 한 장은 문턱을 넘긴다」 **보장 슬롯은 카드 재설계(2026-08-10)에서 개념째 없어졌고**
> (도장이 없으면 「문턱 넘김」이라는 것이 없다), 근거로 든 산수도 90장 풀의 것이라 지금 52장 풀에는
> 안 맞는다. 처방 둘(보장 슬롯 손질 · 문턱 1단을 3→2)도 그래서 무효다. 경위는 `session_logs/2026-08-10.md`.
> **그래서 지금 체감이 어떤지는 아무도 모른다** · 아래는 「다시 재라」이지 「고쳐라」가 아니다.

- [ ] **(2026-08-12 실측) 체감은 이제 반대 방향으로 무너져 있다 · 등급 분포 붕괴.** growth 16시드의
  드래프트 후보 3530장에서 **흔함 86.6~95.2% · 드묾 11.5→1.4%(시대가 갈수록 줄어든다) · 귀함 0.0% ·
  아주 귀함 0.0% · 전설 2.0~3.3%.** 전설이 귀함·아주 귀함보다 자주 뜨는 **서열 역전**이다
  (cards.test 의 서열 검사는 가중치를 재고, 실제 등장은 장수 구성 × RARITY_BOOST 가 정해서 새는 것).
  「귀하다는 느낌」 이전에 **귀함·아주 귀함 열세 장이 사실상 게임에 없다.**
- [ ] **폰에서 다시 판정한다.** 지금 풀은 52장(특성 45 + 열쇠 7)이고 등급별 장수는
  **흔함 16 · 드묾 13 · 귀함 9 · 아주 귀함 7 · 전설 7**. 한 등급이 뜰 확률 = `종류 수 × 가중치` 라
  **장수 구성 자체가 체감을 정한다**(레벨 1·최대 레벨의 가중치 합은 `cards.ts` 의 등급표 주석에 있다).
  · 손대기 전에 `cards.test.ts` 의 **서열 검사**(레벨 1·3·5·7·30)를 함께 볼 것 · 한 등급의 장수를 늘리면
    윗 등급이 아랫 등급보다 자주 떠서 **배지가 거짓말**이 된다.
  · 등급은 이제 손으로 안 적는다(`perkRarity` 가 「조건 성립 빈도 × 효과 크기」로 계산) → 조이는 손잡이는
    **장수 구성 · `RARITY_WEIGHT` · `RARITY_BOOST_*`** 셋이고, **배수(효과)를 만지면 등급이 함께 움직인다.**
  · `RARITY_BOOST_FULL_LEVEL` 이 7 → 18 로 바뀌었다. 산수상 같은 레벨에서 보정이 덜 걸리므로 초반 체감은
    낮아졌을 것이지만 **재 본 적은 없다.**
- [ ] 표시 확률이 실제와 어긋날 이유는 이제 없다(보장 슬롯이 사라져 `rarityOdds` 와 `drawCards` 가 같은
  가중치만 본다). 남은 차이는 **판 중 `cardRedundant` 로 빠지는 카드**뿐이고, 대백과가 그걸 주석으로 알린다.
  ⚠ 다만 대백과 확률과 「1. 성장 속도 재측정」은 같은 파일을 만진다 · 그 작업이 끝난 뒤에 재라.

## ★★★★ 티어 재설계 이후 · 이제 남은 것은 코드가 아니라 폰이다 (2026-08-06 구현 완료)

> 구조는 구현됐다: 게놈 v8(도장과 티어) · 조작 확장 · 은근한 보정.
> 경위와 실측은 [`session_logs/2026-08-06.md`](../../session_logs/2026-08-06.md) 세션 4.
> 성장 수치의 단일 진실은 [`src/sim/tiers.ts`](../../src/sim/tiers.ts) 하나다.
> ⚠ 그때 함께 만든 **도장 카드 72장은 v9(2026-08-10)에서 통째로 사라졌다** · 카드는 이제 열쇠와
> 조건부 특성만 준다(`src/sim/perks.ts`). 아래 항목의 「카드」는 그 사실을 반영해 읽을 것.

- [ ] **새로 손끝에 닿는 것 넷을 폰에서 판정한다.** 이번에 조작이 통째로 늘었고, 손끝 값은
  실기 말고는 정할 방법이 없다:
  · **길게 누르기 0.25초**가 명령 휠을 여는 데 적당한가(짧으면 훔쳐보기와 싸우고, 길면 답답하다)
  · **더블탭 회피**가 오작동 없이 잡히는가(300ms · 44px 안)
  · **명령 철회 줄**(하단 96px)이 다른 UI 에 가리지 않는가
  · **지휘봉 탭**(내 개체 20px 안)이 이동 명령과 헷갈리지 않는가
  ⚠ **쿨타임 길이는 폰 실기로만 정해진다.** 쿨타임 동안 할 게 없으면 다시 "보고만 있는 게임"이 된다
  (특수 명령 60~180틱 · 지금은 전부 추정값이다).
- [ ] **도달 시대가 v7 기준선보다 0.5 낮다**(2.5 vs 3.0 · 손 놓은 판). 정복률은 0% → 6.7% 로
  올랐으니 급하진 않다. 폰 체감이 "너무 빨리 진다"면 유지비 계수(0.038)나 무리 0단 값(32)을
  먼저 보라 — 둘 다 출발선에 직접 닿는다.
  ⚠ 만지기 전에 반드시 `npm run probe -- growth` 로 재라. 이번 세션에 원인을 **세 번** 잘못 짚었다
  (기록: known_issues 「티어로 갈아엎을 때 출발선을 같이 낮춰 버린다」).
- [x] ~~**듀오 열 개 중 셋은 아직 화면에서 안 읽힌다.**~~ **해소(2026-08-10).** 듀오가 **드래프트
  카드**가 되면서 「켜진 순간」이 곧 「고르는 순간」이 됐다. 다만 남은 것이 있다:
  · **듀오 설명이 약속하는데 sim 이 안 하는 것 넷** — 늑대의 법 「같이 물면 피해 1.5배」(나눔 몫만
    구현) · 원진 「도는 병이 절반」(미구현) · 먼저 보고 먼저 뛴다 「도망칠 때 더 빨라진다」(미구현) ·
    파수꾼 「한 마리가 본 것을 전원이 안다」(시야각 절반만). **화면이 약속한 것을 세계가 안 한다.**
    앞 둘은 수가 문장에 있어 구현 가능하고, **뒤 둘은 수가 없어 지어내야 한다**(합의 필요).
  · 새로 구현한 「돌진」(격퇴 ×1.6)·「파도」(추격 표적 흔들기)는 **밸런스를 안 쟀다.**

## ★★★ 아직 구현 안 한 확정 사항 — 시험을 「무엇을 지켜라」로

> **[사용자 2026-08-06]** 확정. 「사냥 5회」처럼 계수를 세는 시험을 **세계 위에 목표를 찍는** 것으로
> 바꾼다: 먹이 지대 차지하기 · 표시된 개체 사냥하기 · 지점 밟기 · 험한 곳에서 버티기 · 전원 생존.
> 2026-08-02 폰 지적("뭘 하려는 건지 모르겠다")의 답이기도 하다.

- [ ] 이번 세션에는 **이빨 0단 종에게 「사냥」 시험이 안 뜨게** 하는 데까지만 했다(초식 거인 경로가
  구조적으로 막히던 자리 · 사냥 효율이 정확히 0 인데 불씨는 다섯뿐이었다).
- [ ] 목표 마커는 sim(월드에 목표 좌표) + 렌더(표식) + goalBar(진행도)가 함께 움직여야 하는 작업이다.
  그때 시험 상수(사냥 5 · 먹이 45 · 새끼 8 · 무리 하한 8)가 전부 재설계 대상이 된다.

## ★★★ 지수 성장·지수 난이도 1단계 뒤에 남은 것 (2026-08-05 4차)

> 값과 계약은 섰고(1단계), 그것이 화면에서 읽히게도 했다(2단계: 관문 생존 칩 · 시대 전환 연출 ·
> 막대 눈금과 정점선 · 카드 직후 성장 한 줄 · 정점 보상 둘의 월드 표식).
> 실측은 세션 로그와 커밋 본문에. **남은 것은 폰 실기 체감과 아래 항목들이다.**

- [ ] **소리가 필요한 자리**(2단계에서 눈으로 읽히게 한 사건들에 아직 소리가 없다). 우선순위 순:
  ① 카드를 고른 순간(성장) ② 정점 도달(가장 큰 사건) ③ 시대 전환(세계가 험해짐) ④ 생존 칩이
  danger 로 넘어가는 순간(경고) ⑤ 관문 통과·실패. ⚠ 소리는 "선택에 결과가 없다"의 답이 아니었다
  (실측 결론) — 눈으로 읽히는 것이 먼저고 이건 그 다음이다. `godot-audio-pipeline` 스킬은 이 프로젝트
  (웹·Pixi)에는 안 맞으니 웹 오디오로 따로 설계할 것.
- [ ] **4단(규칙 면제) 다섯 중 둘만 월드에 표식이 있다.** 다리 4단(뒤로 흐르는 금빛 잔상)·눈 4단
  (금빛 전방위 원)은 붙였다. **이빨 4단(체급 무시)·가죽 4단(환경 면제)·무리 4단(즉사 무효)은 여전히
  화면에 아무 표시가 없다** — 물기·대멸종·포식 순간의 연출로 붙일 자리다.
- [ ] **프로브의 `--drive` 봇이 손 놓은 판보다 나쁘다**(정복 0/32 · 시대 1 개체 5.8마리 vs 손 놓음 17.2).
  150틱마다 먹이 뭉치로 무리를 몰지만 지시가 먹이 추적을 이겨 무리가 행군만 한다. 이 상태로는
  "조종하는 사람"의 밸런스를 잴 수 없다 → 봇을 고치거나, 조종 밸런스는 폰 실기로만 판정한다고 못박기.
  ⚠ 이 결함은 이번 변경 **이전에도 같았다**(기준선 측정으로 확인) — 새로 생긴 문제가 아니다.

## ★★★ 첫 판을 친절하게 (2026-08-05 회의에서 확정 · 이게 먼저다)

> **회의 전문과 근거 수치는 [`../../session_logs/2026-08-05.md`](../../session_logs/2026-08-05.md).**
> 폰 실기 결과: 저장 데이터를 지우고도 첫 시대에서 멸종 · "다짜고짜 복잡한 세계에 던져지는 느낌".
> 진단: 메타 언락이 "선택지"만 늘리게 설계돼 **세계의 복잡도는 첫 판부터 최대**다(로그라이크 관례와 반대).

- [ ] **1순위 · 색과 그림.** 색은 **세 가지 뜻만** 가리킨다(내 무리 / 먹을 수 있는 것 / 나를 먹는 것).
  뒤 시대에 종이 늘어도 새 종은 고유색이 아니라 그 셋 중 한 계열을 받는다. 배경은 낮은 채도.
  · 사용자에게 참고 그림·게임·에셋이 **없다.** 레퍼런스를 묻지 말고 **시안 두세 개를 뽑아 고르게 한다**
    (이미지 파일이 하나도 없고 전부 절차적 생성이라 가능하다).
  · 다크 베이스("어두운 배경 위에 조명 받은 오브젝트")는 **사용자가 게임 허브에 이미 정해 둔 원칙**이다.

**아직 안 정한 것:** 「불씨」의 이름(사용자: 애착 없음, 다만 문구 톤이라 합의해서 정한다) ·
기후를 관문 자리로 올리고 보스를 시대 최종으로 내릴지(2026-08-04 회의 제안, 판단 보류).

**세계 좁히기·맵 축소 뒤에 실측으로 드러난 것(다음에 판단할 것):**

> 용어: 지금 세계의 복잡도는 시대가 아니라 **온보딩 진도**(= min(3, 끝낸 런 수 + 시대))가 연다.
> 진도 0 = 처음 하는 사람의 첫 시대(가장 좁은 세계) · 진도 3 = 온전한 세계. `npm run probe -- steps`.

- [x] ~~**진도 0 무리가 18마리에서 50~75마리까지 부푼다.**~~ **무효 (2026-08-06).**
  근거였던 **"12~20마리 한 무리에 애착"** 이라는 전제를 **[사용자]** 가 폐기했다
  ("소수를 관리하는 것보다 전체 종을 컨트롤하는 게임으로 가는 게 더 재미면에서 나을 것 같아").
  **개체 수가 많아지는 것을 결함으로 취급하지 않는다.** 먹이 수·번식 문턱·진도별 상한을
  "애착 규모"를 근거로 내리지 말 것.
- [ ] **진도 0 보스가 사실상 무료다.** 실측(8시드 · 지시 없음): 5종 보스 전부 8/8 격퇴, 격퇴에
  걸린 시간 중앙값 124~252틱(4~8초). 무리가 30마리를 넘으니 전사도 30명이라, 붙는 순간 끝난다.
  다만 공격력 문턱(55)은 여전히 살아 있다(44·50 은 0/8) · "형질을 안 키우면 못 잡는다"는 규칙은 유지.
  첫 판이 친절해야 한다는 목표와는 맞지만, **보스가 무엇인지 배울 시간이 4초뿐**인 것은 따로 볼 문제다.
- [ ] **시험(진도 1 부터)의 이빨이 빠졌다.** 맵이 작아진 만큼 같은 16초에 더 많이 먹고 더 많이 낳는다
  (진도 1 대조군 먹이 86.5 · 사냥 13.0 · 새끼 11.5 vs 옛 세계 55.8 · 5.5 · 6.2 — 합격선은 45 · 5 · 8).
  60~90% 합격률을 노리고 잡은 목표치라 지금은 대부분 통과한다 → `GAME.trialFeedN` 등을 진도별로
  다시 잡거나 목표를 면적에 비례시킬 것.
- [ ] **진도 2 가 진도 3 보다 빡빡하다**(실측 `probe steps` 8시드 · 채집 한 판 뒤 내 종 14.0 vs 18.8).
  진도 2 에서 종이 5 → 13 으로 한꺼번에 늘어나는데 맵은 1.7 배라, 개체 밀도가 진도 3(2.0배)보다
  37% 높다(먹이는 면적 비례라 함께 안 는다). 계단으로는 단조 증가가 맞지만 **체감 난이도만 여기서
  솟는다.** 레버: 진도 2 배율을 2.0 으로 올리거나(그러면 진도 3 은 챔피언만 추가) 진도 2 에서
  야생 몇 종을 진도 3 으로 미루는 것. ⚠ 후자는 물 많은 세계(군도·대양)에서 바다가 텅 비는 부작용.

**후속 후보:**
1. **이산 시너지 문턱**: 형질 조합이 "켜지는" 순간(예: 속도 60 + 무리 60 → 몰이 사냥).
   지금은 형질이 전부 0~100 연속이라 무엇도 켜지지 않는다. sim 행동 신설이라 프로브 필수.
2. **지도 칠하기**: 서식 범위가 지도에 남는다(축적의 가시화. 전염병 주식회사의 "지도가 곧 진행 바").
3. **라운드를 가로지르는 장기 항목 하나**: 문명의 "겹치는 타이머"(예: 3라운드 뒤 결실 카드).
4. **시대별 시험 난이도 상승**: 지금은 목표치 고정(난이도는 관문이 맡는다).

- 탭 조종은 버리지 않는다. 판정을 수행하는 손(롤토체스의 "배치")으로 역할이 재정의됐다.
- **조종 심화(플레이어 조작 대쉬·시야 안개·포섭·알파 외형)는 계획에서 걷어냈다**
  (**[사용자 2026-08-11]** "플레이어 조작 관련은 일단 싹 정리해버리고" · `parked_ideas.md` 로 이동).
- 진단 원문·문명/전염병 주식회사 논의 전량: `parked_ideas.md`(원문은 git 2992c48) + 세션 로그 08-03.

## 무리 지시·보스 격퇴 수정이 남긴 것 (2026-08-04 배포 cbc5555, 미수정분)

- [ ] **보스전이 무손실 확정승이 됐다.** 전사는 `memberKills` 에서 무조건 안 죽는데 지시가 접촉을
  확실하게 만들어, 몰면 6/6 격퇴에 **사망 0.0마리**. 관문 뒤에 대가가 필요하다(반격에 기력 소모 등).
  ⚠ **보스를 시대 최종으로 내릴지 정해진 뒤에** 손댄다 · 지금 고치면 낭비가 될 수 있다.
- [ ] **드래프트가 보스별 약점을 모른다.** "보스에 못 맞섬" 칩은 거짓일 수 있어 제거했다(격퇴 보스 5종 중
  4종은 카운터가 속도·시야·무리·번식). 정확히 말하려면 sim 에 판정기(보스 종류 + 게놈 → 맞섬 여부)를
  두고 UI 가 그걸 부른다. 규칙을 UI 에서 옮겨 적으면 그 순간 두 곳으로 갈라진다.
- [ ] **군도 맵이 구조적으로 훨씬 어렵다** (균형 잡식 x 약탈자: 대륙 8/8 vs 군도 2/8 · 무흠집 6/8).
  섬이 갈리면 땅 떼와 땅 무리가 아예 안 만난다. `bossEligible` 이 층위만 보고 **도달 가능성**을 안 본다.
- [ ] **챔피언이 내 무리를 밀어낸다.** 먹이 3종을 전부 먹는 잡식(`foodKinds: [0,1,2]`)이라 내 종과 니치가
  100% 겹치는데 우호 편이라 아무도 안 잡아먹는다. 실측(100초·8시드): 내 종 22.9 → 9.8~11.9마리,
  2마리 이하로 무너진 시드 0/8 → 1~3/8. **약한 챔피언이 우승 챔피언보다 더 해롭다**(세기가 아니라 겹침).
  야생종은 먹이 종류를 쪼개 공존시켜 놨는데 챔피언만 그 규칙 밖이다 → 니치를 주면 "예전의 나가 살아있다"는
  재미는 남기고 압박만 뺄 수 있다. ⚠ 다만 챔피언을 지워도 첫 시대 멸종은 그대로였다(유일 원인 아님).
- [ ] 정리 대기: `sim/lead.ts`·`leadVision.ts`·`lead.test.ts`(1200줄)·`leadEnabled` 이름.
  수풀 면역 스위치(`bossCanHunt` 의 `lead.commanded`)가 호출부 0이라 영영 false 인 것도 여기서 함께.

## 내 종 스프라이트 텍스처가 개체마다 새로 만들어지고 런 내내 안 비워진다 (2026-08-03 발견, 미수정)

`render/worldView.ts` 의 `genomeSignature` 가 형질을 `Math.round(t[k] * 20)` 으로 버킷화한다.
형질이 0~1 소수이던 시절의 식인데, 지금은 0~100 정수라 **버킷이 형질 0.05 단위**다. 내 종 새끼는
태어날 때 `mutationStrength 1.5` 로 변이하므로 사실상 **개체마다 키가 다르고**, `texCache` 는 상한 없는
Map 이라 런이 끝날 때까지 안 비운다. 야생종 쪽(`wildGenomeSignature`)은 `/12` 버킷이라 멀쩡하다.

- 결과: 새끼가 태어날 때마다 렌더 타깃 텍스처가 하나씩 생겨 GPU 메모리가 단조 증가. 폰에서 후반
  라운드가 무거워지는 원인 후보.
- 눈으로는 어차피 형질 1.5 차이를 못 알아본다 -> 버킷을 야생과 같은 수준으로 굵게 하면 개체 하나하나의
  텍스처가 사라지고 캐시가 유한해진다. 다만 **개체 변이가 겉모습에 안 드러나게 되는 문제**와 한 쌍이라
  (그게 이 게임의 특색 중 하나), 굵게 하면서 "무엇이 개체차로 보여야 하는가"를 같이 정할 것.
- 범위: `src/render/worldView.ts` (레인 D).

## 난잡함 정리 · 결정이 필요해 남긴 것 (2026-08-03 전수 색출, 근거 있음)

죽은 코드·거짓 주석·죽은 CSS 는 걷어냈다(97257fe). 아래는 **설계 판단이 필요해 손대지 않은 것**이다.

- **`?watch` 관전 모드는 존치 확정** (2026-08-03 사용자: "문제가 생겼을 때나 dev log 용으로
      남아 있었으면 좋겠다"). 지우지 말 것. 지금도 값을 하고 있다: `sim/lead.test.ts`(1200줄)의
      대조군이고, `scripts/overlap-check.mjs` 가 드래프트 화면을 빨리 띄우는 통로다.
      · 대신 **살아 있는지 계속 확인되어야 한다** · 겹침 검사가 매번 `?watch` 로 도니 자연히 확인된다.
      · 유지비: `main.ts` 의 leadMode 게이트 15곳 + worldView 의 시야 부채꼴 분기 하나.
- [ ] **`world.lead.biteTargetId`.** 매 틱 계산되는데 읽는 곳이 테스트뿐이다(사냥 버튼이 사라졌다).
      지울지, "탭 잠금 후보"로 이름을 바꿔 화면에 다시 붙일지. 테스트 9건이 걸려 있다.
- [ ] **어휘가 갈라진 곳** · 화면 문구는 정리했다(cd17e65: 시험=라운드 시험만 / 보스·대멸종=관문 /
      갈래=시작 종 계통만 / 해금 화면="레벨마다 열리는 것"). **코드 주석에 남은 것만 남았다**:
      `cards.ts` 의 "능력 관문 카드"가 보스 관문과 같은 낱말을 쓴다 → "문턱 카드"로 바꿀 것.
      원인 분석: 셋 다 영어 한 낱말(gate·trial·branch)을 그대로 옮겨서 갈라졌다.
      (아래는 정리 전 기록)
      · "관문" 이 두 뜻 (보스 단계 / 능력 문턱 카드)
      · "시험" 이 두 뜻 (라운드 시험 / 대멸종 "큰 시험") · 같은 목표 줄 자리에 번갈아 뜬다
      · "갈래" 가 두 뜻 (시작 종 계통 / 해금 사다리 화면 이름 "진화 갈래")
      · 화면은 "보스"와 "시련"을 가르는데 **대백과는 10종을 전부 "보스"로 묶는다**
- [ ] **진척 지표가 12개다.** 시험·불씨·레벨·개체 수·시대·남은 시간·격퇴 바·미니맵·메타 레벨·
      해금 사다리·도전 과제·런 보고서. 상시 화면의 중복(레벨 XP 이중 표시)만 걷었다.
      메타 진행이 3중(종료 화면 애니메이션 · 열람 사다리 · 대백과 탭)인 것부터 줄일 후보.

## 그래픽 개편 (2026-08-03 사용자 질문: "대대적인 개편이 불가능한가?")

**가능하다. 에셋이 아니라 코드다.** 이미지 파일이 하나도 없고 생물·지형·이펙트를 전부 절차적으로
그린다 → 아티스트 의존이 없고, sim 을 한 줄도 안 건드리고 바꿀 수 있다(시뮬·렌더 분리가 강제돼 있다).

- 범위: `src/render/` 2,692줄. 그중 `worldView.ts` 1,556줄이 대부분(지형 칠하기 + 생물 텍스처 생성).
  `creatureLook.ts` 115줄이 개체별 개성(무늬·눈·크기·색조)을 담당한다.
- **가장 크게 달라 보일 한 곳: 지형.** 지금은 타일 격자를 단색 사각형으로 칠해 "디버그 타일맵"처럼
  보인다. 라운드·판정이 붙은 지금 게임의 인상과 안 맞는 게 여기다. 타일 경계를 부드럽게 하거나
  (셀 보간·가장자리 블렌드), 바이옴별 질감·잔물결을 얹는 것부터가 비용 대비 효과가 크다.
- 검증: 스크린샷 비교 + `npm run overlap`(글씨는 안 건드리므로 회귀만 확인). 프레임은 폰 실기로 봐야 한다.
- ⚠ 먼저 정할 것: **무엇처럼 보이고 싶은가.** 지금 UI 는 "따뜻한 흙빛 유리 패널"인데 월드는 차가운
  타일이라 둘이 따로 논다. 방향을 정하지 않고 손대면 또 임의로 정한 페르소나가 된다(2026-07-09 교훈).

## ★★ 탭 명령 조종 — 폰 실기 검증(전환 직후, 이게 먼저다)

> **무엇인가:** 게임이 "가만히 보고만 있게 된다"는 문제의 방향 전환이 끝났다. **조종이 기본값**이고,
> 내 무리의 알파(주인공) 한 마리를 **탭 명령**으로 이끈다. 나머지는 자율로 살면서 알파를 따라온다.
> 탭한 지점의 개체가 사냥할 수 있는 상대면 **사냥 명령**(잠금 → 추격 → 물기), 그 밖의 모든 지점
> (빈 땅·내 무리·못 사냥하는 상대)은 **이동 명령**이다. 명령은 한 번에 하나, 새 탭이 이전 것을 대체한다.
> 카메라는 알파에 고정(기본 줌 2.2)이고, 훔쳐보기(드래그·미니맵·2손가락 팬)는 손을 뗀 뒤 1.5초면
> 알파로 돌아온다. 유지되는 조작은 배속·멈춤·미니맵·WASD·`E`.
> `?watch` 를 붙이면 조작 없는 예전 관전 세계로 돈다(밸런스 비교·프로브용 탈출구, 임시).
> 무리가 따라오는 세기는 여전히 `?follow=<수>` 로 배포 없이 시험할 수 있다(기본 0.68).
>
> (플레이어 조작 확장 후보였던 시야 안개·대쉬·포섭·알파 외형은 **[사용자 2026-08-11]** 지시로
> 계획에서 뺐다 · `parked_ideas.md`.)

**이 전환으로 폐기된 것**(다시 꺼내지 말 것):
- **조이스틱 킬게이트 3문항**(무리를 끄는 맛 · 화면이 말해 주는가 · 사냥이 재미있는가) — 스틱 손맛을
  판정하기 전에 **사용자가 스틱을 버리기로 결정**해서 판정 대상 자체가 사라졌다.
- **사냥 버튼**(우하단 맥락형) — 탭이 곧 사냥 명령이라 버튼이 필요 없다.
- **한 마리 관찰**(개체 카드·즐겨찾기 별·개체 순환 키·줌 버튼) — 관찰 기능을 통째로 제거했다.

**이 전환으로 해소된 것:** "먹잇감 브래킷은 뜨는데 버튼은 안 켜진다" — 겨눔 범위(`leadTargetRange`) 밖의
브래킷을 흐리게 그려 "지금 물 수 있는 것"과 "보이기만 하는 것"이 갈린다. **폰에서 눈으로 확인만 남았다.**

- [ ] **새 폰 검증 (반드시 1x 배속 · 폰 · 실기).** 코드로 대신할 수 없는 판정이다.
  · **① 탭 이동이 손에 붙는가** — 누른 곳으로 제대로 가는지, 못 가는 지형이나 길이 없는 곳을 눌렀을 때
    거부 핑이 "안 된다"로 읽히는지, 도착해서 깔끔히 멈추는지.
  · **② 탭 사냥(잠금 → 추격 → 물기)이 읽히는가** — 겨눔 범위 밖 상대를 탭해도 알파가 달려가 잡는지,
    잠근 상대가 죽거나 길이 끊기면(물 건너 도망 등) 거부 핑과 함께 잠금이 풀리는 게 납득되는지,
    도중에 옆 개체로 새지 않는지.
  · **③ 기본 줌 1.8 에서 생물 크기·밀도가 맞는가** — 2.2 는 "너무 크고 밀도가 높다"(2026-08-02 실기)로
    한 단계 내렸다. 여전히 크면 더 내리고, 탭이 빗나가면 pickEntity 반경만 올린다(줌과 독립).
  · **④ 훔쳐보기 1.5초 자동 복귀가 답답하지 않은가** — 지도를 보다 끌려 돌아오는 게 성가시면 시간을 늘린다.
  · **⑤ 미니맵 알파 점이 읽히는가** — 우상단 미니맵에서 내가 어디 있는지 한눈에 잡히는지.
  · **⑥ 목표 한 줄(goalBar)이 제 역할을 하는가** — "내가 뭘 해야 하는지"가 이제 읽히는지, 채집/보스/
    큰 시험에서 문구가 맞게 바뀌는지, 탭하면 열리는 상세 패널(레벨·개체 수·배속·내 형질·대백과)이
    쓸 만한지. 이 한 줄이 "뭘 하려는 건지 모르겠다"(2026-08-02)에 대한 대답이다.
  · **⑦ 격퇴 체력 바가 보스 몸 위에서 읽히는가** — 상단 글로벌 바를 없앴다. 보스가 화면 밖이면 바도
    안 보이는 게 맞는 설계인지(미니맵 빨간 점 + 목표 한 줄이 대신 알려 준다).
  · **⑧ 야생 사건 다이어트가 과하지 않은가** — 야생끼리 탄생·자연사는 연출 생략, 사냥·발사체는 30%
    밝기다. 세계가 죽어 보이면 0.3 을 올린다(`effects.ts` dim).
  · 안 읽히는 게 있으면 그게 다음 작업이다.
- [ ] **잘려 나간 정보의 부활 여부(폰 체감 후)** — 타임라인(보스/멸종 예고 마커)·종 안내 범례·사망 원인
  요약은 HUD 갈아엎기에서 화면 밖으로 나갔다. 아쉬우면 goalBar 상세 패널 안에 축약형으로 부활시킨다
  (상시 화면으로는 되돌리지 않는다).

- [ ] **★ 조종 ON 밸런스는 아무것도 검증 안 됐다 — 이제 모든 런이 조종 ON 이다(시급도 격상).**
  기존 테스트는 전부 조종 OFF(`?watch`) 경로다. `src/sim/leadBalance.test.ts`(ON/OFF 개체 수 평균비
  ≤ 1.8, 대멸종 세계 생존 차이 ≤ ×1.3)가 서기 전에는 "조종이 형질을 대체하지 않는다"를 주장하면 안 된다.
  ⚠ 수풀 엄폐 봉인 효과는 분리해서 재야 한다.
  ⚠ **경험치 상한 `GAME.leadStageXpCap`(80)이 이제 예외 없이 모든 런에 걸린다.** 조종이 옵션이던 때는
  틀려도 티가 안 났지만 지금은 추정치 하나가 전체 진행 속도를 정한다 → 실제 단계별 섭취량을 재서 조정.

## ★ 진행 중인 판 저장(새로고침 복구) — 2026-07-16 사용자 요청
- [ ] **새로고침하면 하던 판이 통째로 날아간다** — 사용자: "이미 하던 판 정보가 싹 날아가는 게 너무 아까워".
  진행 중인 런(게놈·레벨·시대·단계·고른 카드·월드 시드·개체 상태)을 localStorage 에 저장하고 부팅 시 복구.
  ⚠ **결정론이 열쇠**: 월드는 (시드+게놈+틱 수)로 재현되므로, 개체 위치를 전부 저장하지 않아도 시드·게놈·
  누적 틱·런 상태만 저장하면 같은 상태로 되돌릴 수 있다(단, 야생 진화·rng 스트림 위치까지 복원해야 완전).
  간단판: 라운드(드래프트) 경계에서만 스냅샷(게놈·레벨·시대·큐)을 저장 → 새로고침 시 그 라운드 시작으로
  복구(관전 중간은 잃되 판은 이어감). meta.ts/achievements.ts 의 캐시-사본 패턴 참고(사생활 모드 안전).

## ★ 최우선 — 보스 레이드 후속 (1·2단계 완료, 2026-07-16)

> **완료:** 1단계 공격 카운터(약탈자, 전사 반격) + 2단계 초식 카운터(속도·무리·시야·번식) 격퇴 + 격퇴
> 체력 바 렌더. era 1+ 만(era 0 보존). 독 안개(전역)만 버티기 유지. 상세는 세션 로그 2026-07-16. **남은 것:**
>
> - **처치 보상 + 버티기 페널티** — 처치=강력 보상 / 버티기 통과=다음 척박 등(현재는 둘 다 그냥 통과).
>   game.ts finishStage 의 bossDefeated 플래그가 이미 있으니 그 갈래에 보상/페널티를 붙이면 된다.
> - **레이드 폰 체감(2026-07-16 구현 후 대기).** 볼 것:
>   · **격퇴 체력 바가 폰에서 읽히는가** — 보스 위 바(46×5px)가 작은 화면에서 보이는지, 줄어드는 게
>     체감되는지. 안 보이면 크게/두껍게. 바가 "왜 줄어드는지"(내 무리가 카운터를 시연 중)는 예고 배너에
>     기대는데, 관전 중에도 알아채는지(안 되면 바 옆 카운터 아이콘 검토).
>   · **각 카운터가 다르게 느껴지는가** — 속도(따돌리며 격퇴)·무리(뭉쳐 격퇴)·시야(미리 봐 격퇴)·번식(수로
>     격퇴)이 실제로 자기 형질로 잡는 맛이 나는지. 미스매치 빌드는 못 잡고 버티기로 통과하는 게 명확한지.
>   · **무리(외톨이) 카운터가 약한 게 괜찮은가** — 프로브상 4/6 격퇴(안 뭉치는 시드는 버티기로 통과).
>     cohesion 상 무리가 잘 안 뭉쳐서다(세션 5). 폰에서 "뭉쳐서 잡는" 맛이 약하면 herdShieldTarget↓ 또는
>     cohesion 튠. 단 무리는 지지 않으니(버티기 통과) 급하진 않다.
>   · **시대별 난이도** — diffMul 이 maxHp 를 키우지만 wear 는 안 키워, 시대가 오를수록 격퇴가 어려워진다
>     (프로브: 무리·번식은 diffMul 2 에서 3~4/6). 후반이 너무 빡세면 raidWearRate↑ 또는 wear 에도 diffMul.

---

> ⚠ **여기부터 아래는 2026-07 의 오래된 이월 항목이다** · v8 티어 재설계·v9 카드 재설계 **이전**에 쓰였다.
> 그래서 **지금 코드에 없는 것**을 근거로 드는 줄이 섞여 있다. 대표적으로:
> 형질 0~100(카드로 200) 연속값 · 정점(100 고정)과 감쇠 취소선 · `sacrifice` · `CARD_GROWTH_SCALE` ·
> 도장 카드와 갈래 전용 카드 풀 · 개체 정보 카드/단골 별/「한 마리 관찰」(UI 통째로 제거됨).
> **아래 항목에 손대기 전에 그 심볼이 아직 있는지 먼저 확인하라.** 되살릴 때는 v9 어휘
> (도장 · 단(티어) · 열쇠 · 조건부 특성)로 다시 쓰고 낡은 줄은 지운다.

## 다음 세션 착수 (2026-07-14 사용자 지시 — 남은 것)

> ⚠ **여기 있던 ②(초음파 시야 카드 낚시) · ③(카드 풀 대폭 확장) · ⑤(채집 절벽)는 지웠다.**
> ②·⑤ 는 그때 끝난 항목이고(기록은 `session_logs/2026-07-15.md`), ③ 이 말하던 **카드 풀 자체가
> 두 번 갈아엎혀 없어졌다**(v8 도장 카드 → v9 특성 카드 52장). 거기 딸린 「정점 희생 관문」도
> 형질이 0~100 연속이던 시절의 장치다 · `sacrifice` 는 지금 코드에 없다.
> 형질 정점(100 고정)·감쇠 취소선 역시 티어 구조로 바뀌며 사라진 개념이라 이 절에서 걷어냈다.

- [ ] **⑥ 화면 안에서 알아채게 — 대백과 의존 걷어내기(상시 규칙, CLAUDE.md). 남은 것:**
  · **무리 방어 — 해결됨(2026-07-15 세션 5).** 방패가 선 개체에 파란 보호 링(worldView). 폰 대비 튠만 남음.
  · **몸집·은신** — 크기/흐릿함으로 보이긴 하나, 그게 **왜 이득인지**(안 잡아먹힘·늦게 발견됨)는 안 보인다.
  · **조건부 특성이 「지금 켜졌다」가 안 읽힌다**: v9 에서 새로 생긴 가장 큰 구멍이다(위 착수 순서 2번).
  · 「대백과에서 더 보기」류의 가벼운 유도를 어디에 몇 번 띄울지도 함께 설계.
  ⚠ 옛 항목 「초음파의 시야 희생」은 뺐다. 지금 초음파 열쇠는 시야를 0 으로 만들지 않는다
  (`KEY_DESC.echo` = "어둠 속에서 소리로 봅니다. 대신 밝을 때 눈이 부십니다").

- [ ] **정점 폰 체감(2026-07-15 구현 후 대기).** 정점은 한 시대에 거의 못 닿는 **장기 목표**로 설계됐다
  (프로브 A/B: 프리셋 도달 단계가 거의 안 움직였다 = 대부분의 런에서 안 걸린다). 폰에서 볼 것:
  · 정점이 **너무 안 잡히는가** — 한 런에 한 번도 못 보면 존재하지 않는 기능이다. 그러면 감쇠
    (`FALLOFF_POWER` 0.5)를 완화하거나 시대 보상(×2)을 정점 쪽으로 기울인다.
  · 취소선(`~~+14~~ +6`)이 폰 카드 폭에서 안 깨지고 읽히는지. 각주 두 줄이 카드를 너무 길게 만들지 않는지.
  · 정점 도달 연출(금빛 고리 + "정점 / 속도 100 / 험한 땅도 발을 잡지 못해요")이 관전 화면에서 충분히
    눈에 띄는지, 아니면 너무 요란한지(2.2초).

## 그 다음 (v7 폰 체감)
- [ ] **게놈 v7 폰 체감(2026-07-14 구현 후 대기) — 가장 급한 확인.** 무리 성향이 능력 형질로 내려가고
  **몸집·은신**이 새로 들어갔다. 폰에서 볼 것:
  · **몸집이 크기로 읽히는가** — 「커다란 몸」을 뽑으면 생물이 눈에 띄게 커지는지(배수 0.6~1.4).
    커진 종이 실제로 안 잡아먹히는 게 보이는지. 「작고 날쌘 몸」은 작아 보이는지.
  · **은신이 흐릿함으로 읽히는가** — 최대 45%까지만 흐려진다(내 무리를 봐야 하니까). 너무 약하면 강도↑.
  · **무리 방어는 여전히 화면에서 안 읽힌다** — "포식자가 안 오는 것"은 눈에 안 보인다. 방패가 선 무리
    표식(둘레 옅은 링?)이나 포식자가 가장자리에서 **돌아서는** 연출이 필요(`render/worldView.ts`).
  · 세 방어 갈래(뭉치기·커지기·숨기)가 실제로 다르게 느껴지는지.
- [ ] **「느긋한 정찰자」가 가장 약하다**(도달 3.6 · 잡아먹힘 35 — 최다). 이제 은신·몸집 카드로 살릴 여지가
  생겼다(무리 성향 없이도 방어 갈래가 둘 늘었다). 카드 풀에서 실제로 뽑히는지 확인 후 판단.
- [ ] **「거대 초식」 프리셋 검토** — 몸집이 다산의 대립축이 됐으니(작고 많이 낳기 ↔ 크고 안 잡아먹히기)
  시작 프리셋으로 세울 만하다. 지금은 카드로만 갈 수 있다. 프리셋 추가는 UNLOCK_TIERS·presetPanel 동반.
- [ ] **몸집을 야생종에도 줄지** — 지금 야생은 전부 몸집 50(중립)이라 "큰 야생 짐승"이 없다. 코뿔소·들소
  같은 큰 초식이나 작고 빠른 설치류를 넣으면 생태가 두터워진다. 단 아키타입에 size 를 주는 것뿐이라
  rng 스트림은 안 밀린다(species.ts 가 이미 rng 없이 설정).
- [ ] **육식 사냥꾼이 여전히 최강**(대륙 5.3). 구조적 원인: ① 사냥은 먹잇감이 야생 100마리라 사실상 무한한데
  채집은 유한한 먹이를 야생 초식 넷과 나눈다 ② **식성 특화 페널티가 30%뿐이라** 사냥꾼(식성 68)이 사냥
  효율 98% + 채집 효율 72% 로 **양쪽을 다 먹는다**. `dietSpecializationPenalty` 를 올리면 대멸종 필터가
  깨진다(프로브: 0.4 부터 경계) → predationEnergy 하향이나 채집 수입 상향 쪽을 프로브로 검토.
- [ ] **바다 야생종 확충.** 지금 바다에 사는 야생은 둘뿐(바다 풀뜯이·물고기 떼). 대양·군도 맵에서 육상
  야생 6종의 서식지가 줄어 생태가 한쪽으로 기운다. `WILD_ARCHETYPES` 직접 추가는 rng 스트림을 밀어
  밸런스가 통째로 이동하므로 **독립 rng 스폰**으로 넣어야 한다(known_issues).
- [ ] **대양에서 날개가 못 산다**(도달 1.5). 산이 1%라 고산 먹이가 없다. 나는 종이 얕은 바다 먹이를
  낚아채게 하면(물수리) 살아나지만 대륙에서도 날개가 강해지므로 프로브 필수.
- [ ] **물 → 뭍 진화 경로가 없다.** 수영을 내리는 카드가 없고, 물 전용 종(수영≥90)은 플레이어가 될 수
  없다(카드 상한 89). "바다에서 시작해 뭍으로 올라오는" 서사가 통째로 빠져 있다(2026-07-13 사용자 지적).
- [ ] **보스 층위 폰 체감 튠(2026-07-13 구현 후 대기).** 날개 빌드가 땅 보스를 통째로 회피하는 게 시원한지,
  대신 만나는 하늘 보스(큰수리·말벌 떼)가 그만큼 무서운지. 나는 종 아래 그림자가 "떠 있음"으로 읽히는지.
  큰수리 색(구릿빛 0xb5642a)이 하늘 개척자 내 종(황금 0xf0c840)과 충분히 구분되는지 — 안 되면 더 갈라야 함.
  상어는 수영 빌드로만 나오니 바다 프리셋으로 확인. 세부 후보: 말벌 속도 저항 0.9·큰수리 visionFlee 60.
- [ ] **하늘/물 보스 수가 얇다.** 나는 종이 만나는 보스는 큰수리·말벌 떼·독 안개 3종뿐(한 시대 관문 2개라
  당장은 충분하지만 시대를 거듭하면 반복된다). 수영 빌드도 상어 1종. 층위별 보스를 더 늘릴 여지.
- [ ] **상어가 작은 웅덩이에 갇힐 수 있다.** `mapHasSea` 는 nearestLargePassable 이 폴백으로 자투리 물을
  돌려주면 true 가 된다 → 바다가 없는 맵에서 상어가 웅덩이에 갇혀 무해해질 여지. 물 타일 수를 직접 세는
  판정으로 조여야 한다.
- [ ] **극단값(상한 0/100 도달) 설계.** 지금은 그냥 멈춤(clamp)+그 형질 카드 빠짐뿐이라 "최고조/바닥"에서
  무슨 일이 나는지 의도가 없다. 상한 100 에 닿으면 뭔가 의미 있는 상태(특화 완성 표식·부작용?)를, 하한
  0 도 마찬가지로 설계. 시뮬 공식엔 이미 바닥이 있으니(속도 0.4배 등) 그 위에 "읽히는 극단"을 얹을지 검토.
  (2026-07-12 사용자 지시 — 스탯 숫자+상한 100 개편 후속)
- [ ] **식성 효율 폰 체감 튠(2026-07-11 연동 후 대기).** 페널티 폭 `dietSpecializationPenalty`=0.3 은 대멸종
  필터가 안전한 상한이다(프로브: 0.4 부터 필터 경계). 폰에서 특화 유인이 약하면(잡식이 여전히 안전빵) 필터
  치사도(`globalColdLethality` 등)와 함께 올려야 한다(세션 7 방식). 「포식/초식 본능」 카드가 이제 사냥/채집
  효율 이득을 주는데, 드래프트·카드 도감에서 그 이득이 안 보인다(효율은 칩에 안 뜸) — UI 노출 검토.
- [ ] `stuckTicks` 재정의 — 지금 "이동량" 기준이라 물가 진동에서 안 걸려 **죽은 코드**다. "목표까지 거리가
  안 줄어듦"으로 바꾸면 살아난다(발동이 늘어 밸런스 이동 → 프로브 확인). 상세: known_issues.
- [ ] 물기 연출(`drawBite`)이 폰에서 읽히는지 + 추격 한 번에 2~7번 떠 시끄럽지 않은지 확인(세션 7).
- [ ] 「거인」·꾸밈·물기 개편 폰 체감 검증(세션 4·7).

## UI 폴리시 (진행 중)
- [ ] **개체 카드 이름 ↔ ★ 겹침(버그)** — 카드 헤더가 좁아(포트레이트 56 + 정보열 ~120px) 개체 이름과
  즐겨찾기 ★ 가 겹쳐 보인다. 이름 폭/줄바꿈 또는 ★ 위치 재배치로 해소. (`ui/creatureCard.ts`, 2026-07-08 사용자 지적,
  **2026-07-13 데스크톱에서도 재보고** — 즐겨찾기를 누르면 별표가 이름 글씨와 겹친다)
- [ ] **즐겨찾기 키 표식(B) 안 보임** — 데스크톱 키 칩(.ui-kbd)을 버튼마다 달았는데 개체 카드
  (`ui/creatureCard.ts`)의 ★·‹›·✕ 버튼에는 안 달았다. 키 자체(B 단골·←→ 개체 넘기기·Esc 닫기)는
  main.ts 관전 레이어로 이미 동작 — 표식만 없다. ★ 버튼에 B 칩(위 겹침 문제와 같은 헤더라 함께 해소),
  ‹›·✕에도 ←→·Esc 표식 또는 title 툴팁. (2026-07-13 사용자 지적)
- [ ] 신생아 amber "핑"이 초록 무리 속에서 충분히 읽히는지 폰 확인 — 약하면 대비/굵기 강화. (`render/worldView.ts`)
- [ ] 단골로 "바로 이동" 버튼(지금은 월드 금빛 별을 찾아 탭). 무리 형질 분포(평균·범위) 표시(§9)도 후보.

## 다음 세션 바로 후보 (2026-07-06 갱신 — 최근 대량 변경 후 상태)
- [ ] **런 보고서 폰 검증·확장** — 구현됨(결과 화면 "이 혈통의 기록": 개체 수 면적 그래프 + 형질 흐름
  꺾은선 + 연대기, `ui/runReportScreen.ts`). 폰에서 그래프 선 겹침·연대기 가독성 확인. 승리/정복(여러 시대
  누적) 런의 보고서도 점검. 여지: 형질별 개별 스케일 토글, 사망 원인 막대를 보고서에도, 챔피언 등재 표식.
- [ ] **보스 절차 아트 — 나중에 다시 논의(사용자 보류)**. 현재 상태: `drawBossShape`가 top-down 실루엣 8종
  (땅: chaser=질주 맹수·swarm=벌레·raider=뿔짐승·isolation=늑대·stalker=어둠 덩어리 / 하늘: raptor=큰 날개
  맹금·hornet=줄무늬 말벌 / 물: shark=유선형+초승달 꼬리), 눈썹 없음, 색 파생 굵은 윤곽선·큰 눈으로 생물과
  통일, 회전 안전. `npm run boss-preview`로 8종 스크린샷 확인. 남은 아쉬움(사용자 재논의 예정): 다리 달린
  4종이 다 벌레처럼 뭉뚱그려질 여지, 작은 크기 다리 굵기, stalker 별 모양, raider 뿔 각도, 상어 가슴지느러미
  가 별 모양으로 읽힐 여지.
- [ ] **최근 변경 폰 검증·튜닝** — 한 번에 많이 바꿔 체감 확인 필요: 시대 난이도 곡선
  (`config.ts` eraDifficultyStep 0.22), 챔피언 등장, 스킵 보상(draftSkipBrood 2),
  승리·정복·멸종 연출 강도(momentOverlay). 약하면/과하면 상수 조정.
  ⚠ 여기 있던 「형질 200 스케일 · `CARD_GROWTH_SCALE` · 카드 수치=실제값 동기화 · 프리셋별 카드 풀 ·
  한 마리 관찰」은 지웠다 · 형질 0~200 도, 그 상수도, 관찰 기능도 지금 코드에 없다.
  ⚠ 여기 있던 「시대 보상 폰 튠(강화 카드 ×2 · `eraRewardBoost`)」도 지웠다 · **강화 ×N 은 v9 에서
  사라졌다**(곱할 도장이 없다). 지금 시대 보상이 얇아진 문제는 **위 착수 순서 1번**이 맡는다.
- [ ] **메타 진행(플레이어 레벨) 폰 튠** — 구현됨: 런마다 성적(도달 레벨·시대·정복)만큼 누적 메타 경험치가
  쌓여 플레이어 레벨이 오르고, 언락(프리셋·카드·리롤)이 레벨 티어로 열린다(`meta.ts`). 런 종료 시 진척도
  화면(`ui/levelUpScreen.ts`)이 경험치바를 채우며 레벨업·해금을 하이라이트. 사다리는 `UNLOCK_TIERS` 가
  단일 진실이다(리롤 Lv2 · 초음파 3 · 바다 갈래 4 · 하늘 6/7 · 원거리 9/10 · 독 12/13).
  경험치 곡선(`META_LEVEL_BASE/STEP`)·적립량(`XP_PER_*`)이 폰에서
  "탕탕탕" 느낌과 해금 페이스에 맞는지 체감 후 조정. `?dev` 패널의 Lv 버튼·"진척도+120"으로 바로 테스트.
  주의: 옛 저장본(bestLevel/runsCompleted)은 metaXp 0으로 초기화됨(디버그로 세팅 가능).
- [ ] **개체별 진화 — 1차 프로토타입 완료, 튜닝·심화 필요**. 새끼가 종 기준선이 아니라 **부모 게놈을 상속 +
  연속 형질 ±변이**(독립 `mutRng`), 카드는 살아있는 무리 전체+기준선에 적용(플레이어가 방향을 쥔다).
  능력형·식성은 변이 제외. `SIM.mutationStrength`=1.5(현재). 남은 일:
  - **변이가 폰에서 눈에 보이는지**(현재 강도 1.5는 개체차가 미묘 — 균일 50 시작 시 t1500에 대사 45~49 수준).
    더 크게 하려면 강도↑ 필요한데, 소수 개체+통과기준 3이라 절대 통과기준 테스트(특히 한파)가 노이즈로
    흔들린다 → 그 테스트들을 **방향 기반(hi>lo)**으로 견고화한 뒤 강도를 올리는 게 정공법(known_issues).
  - **선택압 가시화** — 추운 맵/더운 맵에서 세대가 지나며 대사가 실제로 갈리는지 폰/프로브로 확인·연출.
  - 못 버티는 개체가 솎이는 게 화면에서 읽히는지(연출), 개체 즐겨찾기/계보 등 애착 심화는 그 다음.
- [ ] **날개(비행) 폰 튠** — 비행 대사 대가(flyMetabolismCost 0.25)·시야 보너스(flyVisionBonus 0.5)·
  고산 먹이(mountainFoodPatches 30) 체감 후 조정. 날개 스프라이트가 보이는지, 산 위 흰빛 고산 먹이가
  보이는지, 비행 vs 지상 트레이드오프가 재미있는지. 하늘 개척자 프리셋/날개·튼튼한 날개 카드로 테스트.
- [ ] **야생 고산 종** — 산 위에 사는 비행 야생종(바다 풀뜯이·물고기 떼가 바다에 생명을 주듯 산에도).
  단 `WILD_ARCHETYPES` 추가는 `generateWildSpecies`의 rng 스트림을 늘려 밸런스가 통째로 이동 → kin 처럼
  **독립 rng 스폰**으로 넣어야 한다(별도 설계). 넣으면 밸런스 프로브로 기존 분포 보존 확인.
- [ ] **전투 형질(방어독·원거리) 폰 튠** — 방어독 강도(venomOnHit 100·tickDamage 15 — 잡아먹으면 손해)·
  원거리 사거리(rangedBonus 22) 체감 후. 독 살갗으로 포식이 막히는지, 원거리 사냥꾼이 멀리서 치는지,
  독 걸린 포식자 보라 tint. 방어독이 너무 세면(포식 완전 회피) 강도↓.
- [ ] **산 보물 폰 튠** — mountainFoodPatches 10·treasureSpawn 3·재생 4배 체감 후. 보물 먹으면 동족이
  확 불어나는 대박이 재밌는지. 레벨업으로 바꿀지(현재 동족 증식). 야생 고산 종과 보물 경쟁도 여지.
- [ ] **야생 진화 심화 폰 튠** — 압력별 적응(포식→속도·무리, 먹이부족→저대사, 추위→고대사) 강도 체감 후.
  한 판(≈6000틱) 지켜보면 야생종 형질·스프라이트가 눈에 띄게 갈리는지(작은 풀벌레가 빨라지고 뭉치는지,
  추운 맵서 대사 오르는지). 약하면 wildAdaptRate/Range↑, 너무 세 밸런스 흔들리면↓. 프로브로 분포 먼저.
- [ ] **물고기 학교 + 독 중독 표식 폰 튠** — ① 시작 물고기 15가 학교로 뭉쳐 보이는지·붐버스트가 자연스러운지
  (심하게 사라지면 deepFoodPatches↑). ② 깊은 바다(진한 남청) 먹이가 얕은 청록과 구분되는지. ③ 독 살갗
  프리셋으로 포식자에 보라 오라·방울(중독)이 뜨는지·잘 보이는지. 육지 떼종(작은 풀벌레·들풀) 실체화도 여지.
- [ ] **초음파 폰 튠** — echoBase 130·카드 강도(echo+42 vision-24) 체감 후. 초음파 원(보라)이 잘 보이는지,
  눈 vs 귀 트레이드오프가 재미있는지. 초음파 야생종(두더지·박쥐형) 추가도 여지.
- [ ] **레벨업 속도 재확인(2026-07-14 조정 후 대기)** — 사용자가 "드래프트가 너무 자주 뜬다"고 해
  xpBase 24→40 · xpPerLevel 14→22 로 올렸다(한 런 드래프트 7 → 5.4회). 폰에서 여전히 잦은지 확인.
  ⚠ 근본 문제는 따로 있다: 경험치가 **개체 수로 정규화되지 않아** 무리가 커질수록 레벨업이 빨라진다
  (18마리면 16초 라운드에 30~40개). 폰에서도 잦으면 값이 아니라 이 구조를 손봐야 한다.

## 시련 실재화 (사용자 방향: 시련이 맵·개체·형질과 겉돈다 → 실재화)
> 2026-07-02 개체형 떼 5종 완료(swarm·raider·isolation·stalker는 실제 떼 개체, chaser 는 단일).
> 독안개만 진짜 전역 재난이라 화면 틴트 유지. 남은 후보는 아래.
- [ ] **외톨이 사냥꾼 카운터 밸런스** — 무리성 카운터가 개체 시뮬에서 약함(무리성의 성장 부작용과
  상충, 단일 시드 노이즈). 폰 체감 후 저항/위협 조정. 필요하면 "이웃 수 기반"을 다시 시도(정교화).
- [ ] **험지 감속 강도 튠** — 소수 개체+국소 보금자리라 감속이 세면 내 종 붕괴 → 온건하게(최대 15%)
  둠. 폰 체감상 효과가 약하면(속도 형질 이점 미미) 조금 올리거나, 험지가 내 종 보금자리를 덜 덮게
  스폰 회피. 수풀 감쇠 강도도 폰 체감 후 조정.
- [ ] **지형×형질 더 넓히기** — 수풀(시야)·험지(속도) 완료(0628623·0177f45). 늪(감속+먹이)·개활지
  (시야 보너스) 등 추가 여지. 산은 현재 완전 차단 → "느리게 통과"로 바꾸면 길찾기 대공사(신중히).
- [ ] **개체형 떼 시련 용어** — 이제 5종이 killRadius>0 이라 다 "보스"로 분류("보스 · 약탈자 무리"
  어색). 단일 강력=보스 / 떼=? 로 용어 재정리 검토(폰 피드백 후).

## 생태·형질 깊이 (이월)
- [ ] 포식자-피식자 수 붐-버스트 안정화
- [ ] 순수 육식(diet>0.7) 자생력 — 현재 잡식 경유가 정답(글래스캐논). 재미 점검
- [ ] herding → boids(정렬·응집·분리) 본격화
- [ ] 환경 hazard(위험) 구역 추가

## 바다 맵 (이월)
- [ ] 물 전용 플레이어 본격화 — 바다 넓게 + 바다 먹이 풍부 + 바다 전용 특성. 보정 코드는 world.ts 대기.
- [ ] 바다 풀뜯이 등 수생 종 생물 도감(`ui/glossary.ts`) 반영

## 그래픽 (이월)
- [ ] 생물 애니메이션(꿈틀/지느러미), 죽음/번식 연출
- [ ] 회전 피벗이 몸 중심과 약간 어긋남 — 정렬 보정
- [ ] 더 정교한 아트(현재 makeCreatureTexture 절차 생성)

## 성능
- [ ] 렌더 Graphics 재드로우 → ParticleContainer/스프라이트 (소수 개체 전환으로 우선순위 낮아짐)

## 아이디어 보관 (당장 아님)
- 세대 계보(부모-자식 추적) 애착 심화 — "복잡하다"고 보류(사용자). (개체 즐겨찾기는 2026-07-08 구현됨: 단골 별 마커)
- 게놈 → 위협 요약 자동 생성 (보스 예고로 부분 달성 — bossPreview/bossCounter 가 토대)
