{
  "name": "selection-pressure",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "적자생존 — 한 종을 길러 생태계 정점에 올리는 관전형 진화 로그라이크",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "boss-preview": "node scripts/boss-preview.mjs"
  },
  "dependencies": {
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
